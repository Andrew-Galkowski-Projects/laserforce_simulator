# LG-01h — Global Nav Restructure · Seam Contract

Locked artifact for the three parallel agents (code / tests / docs). LG-01h ships the **mode-based base.html restructure** (a new `core.context_processors.app_mode` driving a `{% if app_mode == "league" %}` / `{% else %}` branch in `templates/base.html`), **19 placeholder pages** behind a single `coming_soon` view rendered through a shared `templates/_placeholder.html` partial (every disabled sidebar entry and disabled top-bar dropdown item from LG-01f flips from `<span class="disabled">` to `<a href>`), and the **sidebar shape expansion from 14 to 23 entries** (1 top + 6 LEAGUE + 4 TEAM + 6 PLAYERS + 6 STATS) via an in-place body extension of LG-01f's `_build_league_sidebar_links`. **No model change, no migration, no simulator touch, no RNG, no `_flush_to_db` touch, no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline, no new ADR, no CONTEXT.md edit, no JS framework, no Celery, no `messages.*`, no API endpoint, no new dependency, no admin change.** This contract mirrors the structure of [`.claude/worktrees/lg-01f-seam-contract.md`](lg-01f-seam-contract.md) (sidebar partial + context processor + DOM-id discipline + helper signature precedent) and [`.claude/worktrees/lg-01g-seam-contract.md`](lg-01g-seam-contract.md) (in-place body extension of `_build_league_sidebar_links` without renaming, and the single-shared-view pattern with a hard-coded feature-key vocabulary). ADR-0017 ([`docs/adr/0017-league-context-nav-shape.md`](../../docs/adr/0017-league-context-nav-shape.md)) is extended at code time by the Docs agent with a Consequences-extension note recording the 14→23-entry shape change, the 4-state sidebar-active enum extension, and the mode-based base.html branching — **the ADR edit is the Docs agent's task; this contract pins the decision but does not edit the ADR**.

## Mode-detecting context processor (Part a)

NEW module `core/context_processors.py` (already created by LG-01f for `league_nav`; this contract **appends** a second processor to the existing file, NOT a new file). Function `core.context_processors.app_mode(request: HttpRequest) -> dict[str, str]` returns the single-key dict `{"app_mode": "league" | "sandbox"}`. Path-prefix rule (locked): `request.path.startswith("/leagues/") or request.path.startswith("/seasons/")` ⇒ `"league"`; everything else (including `/`, `/teams/`, `/players/`, `/matches/`, `/maps/`, `/help/*`, `/tools/*`) ⇒ `"sandbox"`. Read via `getattr(request, "path", "/")` so a `RequestFactory()`-built request without `.path` doesn't crash. Registered in `settings.TEMPLATES[0]["OPTIONS"]["context_processors"]` **immediately after** `core.context_processors.league_nav` (locked order — `league_nav` first, `app_mode` second). Locked literals `"league"` and `"sandbox"` and the locked context key `app_mode`.

## Base.html mode branching (Part a)

`templates/base.html` is MODIFIED — the existing `<div class="navbar-nav ms-auto">` block (LG-01a `Leagues` link replaced by the LG-01f `League ▾` dropdown plus the 6 LG-01a flat sandbox links) is restructured around `{% if app_mode == "league" %}` / `{% else %}`. **League mode** renders, in pinned left-to-right order: the brand link (unchanged, `{% url 'landing' %}`), then `League ▾` (existing LG-01f dropdown, preserved verbatim — see "Top-bar dropdown items" below for the LIVE flip), then `Help ▾` (NEW), then `Tools ▾` (NEW). **Sandbox mode** renders the brand link, then the 6 LG-01a flat sandbox links in their existing left-to-right order (Teams / Players / Matches / Batch Sim / Create Team / Maps — verbatim `<a class="nav-link" href="{% url '…' %}">` from current `base.html` lines 30–35), then `League ▾`, then `Help ▾`, then `Tools ▾`. The LG-01a-locked outer wrapper `<div class="container">` + the `<button class="navbar-toggler">` + the `<div class="collapse navbar-collapse" id="mainNav">` markup is preserved verbatim around both branches. The LG-01a-locked DOM id `leagues-nav-link` on the dropdown toggle and the LG-01f-locked DOM id `league-history-topbar-link` on the History `<a class="dropdown-item">` are preserved verbatim in both branches.

## Top-bar dropdown items (Part a flip)

`League ▾` dropdown (preserved structure, items flip from disabled `<span>` to LIVE `<a>` where placeholder URLs apply): item 1 **Standings** flips to LIVE pointing at `/leagues/<int:league_id>/standings/` resolved via the `top_bar_history_url`-style context-aware chain (see below); item 2 **Playoffs** flips to LIVE pointing at the placeholder URL `coming_soon_playoffs`; item 3 **Finances** flips to LIVE pointing at `coming_soon_finances`; item 4 **History** stays LIVE (LG-01f, unchanged, `{{ top_bar_history_url }}`); item 5 **Power Rankings** flips to LIVE pointing at `coming_soon_power_rankings`. Order locked top-to-bottom: Standings / Playoffs / Finances / History / Power Rankings. DOM ids on each `<a class="dropdown-item">`: `league-standings-topbar-link` / `league-playoffs-topbar-link` / `league-finances-topbar-link` / `league-history-topbar-link` (LG-01f, preserved) / `league-power-rankings-topbar-link`. The Standings / Playoffs / Finances / Power Rankings hrefs need a per-user League-id; resolution chain is identical to the LG-01f `top_bar_history_url` chain — `app_mode` processor SHOULD NOT re-implement this; the existing `league_nav` processor gains **3 new keys** alongside `top_bar_history_url`: `top_bar_standings_url`, `top_bar_playoffs_url`, `top_bar_finances_url`, `top_bar_power_rankings_url` (4 new keys total, all resolved via the same 3-step session-pin → single-League → list-page chain LG-01f locked). When no League exists, every key resolves to `reverse("league_list")` (LG-01f fallback precedent).

`Help ▾` dropdown (NEW, 6 items in pinned top-to-bottom order, ALL placeholder URLs): **Overview** (`coming_soon_help_overview`), **Changes** (`coming_soon_help_changes`), **Custom Rosters** (`coming_soon_help_custom_rosters`), **Debugging** (`coming_soon_help_debugging`), **LOL GM Forums** (`coming_soon_help_lol_gm_forums`), **Zen GM Forums** (`coming_soon_help_zen_gm_forums`). Toggle text `"Help ▾"` (U+25BE), toggle DOM id `help-nav-link`, per-item DOM ids `help-overview-topbar-link` / `help-changes-topbar-link` / `help-custom-rosters-topbar-link` / `help-debugging-topbar-link` / `help-lol-gm-forums-topbar-link` / `help-zen-gm-forums-topbar-link`.

`Tools ▾` dropdown (NEW, 4 items in pinned top-to-bottom order, ALL placeholder URLs): **Achievements** (`coming_soon_tools_achievements`), **Screenshot** (`coming_soon_tools_screenshot`), **Enable Debug Mode** (`coming_soon_tools_debug_mode`), **Reset DB** (`coming_soon_tools_reset_db`). Toggle text `"Tools ▾"`, toggle DOM id `tools-nav-link`, per-item DOM ids `tools-achievements-topbar-link` / `tools-screenshot-topbar-link` / `tools-debug-mode-topbar-link` / `tools-reset-db-topbar-link`.

## Shared placeholder view (Part b)

NEW view `matches.views.coming_soon(request: HttpRequest, feature_key: str, league_id: int | None = None, team_id: int | None = None) -> HttpResponse` is the single shared `<h1>Coming soon</h1>` view for every placeholder URL. Undecorated, GET-only via `if request.method != "GET": return HttpResponseNotAllowed(["GET"])` as the **first** line of the body (LG-01c / LG-01d / LG-01e / LG-01f / LG-01g locked pattern). Body: (1) 405 guard; (2) `if league_id is not None: league = get_object_or_404(League, pk=league_id)` else `league = None`; (3) `team_id` is reserved for future per-Team routes (LG-01h does NOT mount any `<int:team_id>/` placeholder, but the kwarg is in the signature for forward-compat with LG-02+); (4) `feature = _FEATURE_REGISTRY.get(feature_key)` against the module-level hard-coded dict (locked vocabulary below) — `feature is None` ⇒ `Http404(f"Unknown placeholder feature {feature_key!r}.")`; (5) when `league is not None` write `request.session["last_league_id"] = league.id` (extends the LG-01f session-write site list); (6) `displayed_season = league.active_season or league.seasons.filter(state="completed").order_by("-id").first()` when `league is not None`, else `None`; (7) `sidebar_links = _build_league_sidebar_links(league, displayed_season, sidebar_active)` when `league is not None`, else `sidebar_links = []` (Help / Tools placeholders render with an empty sidebar — they are sandbox-mode pages); `sidebar_active = feature["sidebar_active"]` (a literal from the 23-entry enum or `None`); (8) `return render(request, "_placeholder.html", context)` with the locked **6 context keys** `league` / `displayed_season` / `feature_key` / `feature_label` (`feature["label"]`) / `feature_section` (`feature["section"]`) / `sidebar_links` / `sidebar_active`. The view is placed in `matches/views.py` even for the Help / Tools entries (single shared view; no `core/views.py` duplication).

`_FEATURE_REGISTRY: dict[str, dict[str, str | None]]` is a module-level hard-coded dict in `matches/views.py` keyed on `feature_key` with value dicts `{label: str, section: str, sidebar_active: str | None}`. `section` ∈ `{"league", "team", "players", "stats", "help", "tools"}`. The full vocabulary (locked, alphabetical for review but order does not affect routing): League-scoped (10) — `league_playoffs` (label `"Playoffs"`, section `"league"`, sidebar_active `"playoffs"`), `league_finances` (`"Finances"`, `"league"`, `"finances"`), `league_power_rankings` (`"Power Rankings"`, `"league"`, `"power_rankings"`); Team-scoped (3) — `team_roster` (`"Roster"`, `"team"`, `"roster"`), `team_finances` (`"Finances"`, `"team"`, `"finances_team"`), `team_history` (`"History"`, `"team"`, `"history_team"`); Players-scoped (6) — `players_free_agents` (`"Free Agents"`, `"players"`, `"free_agents"`), `players_trade` (`"Trade"`, `"players"`, `"trade"`), `players_trading_block` (`"Trading Block"`, `"players"`, `"trading_block"`), `players_prospects` (`"Prospects"`, `"players"`, `"prospects"`), `players_watch_list` (`"Watch List"`, `"players"`, `"watch_list"`), `players_hall_of_fame` (`"Hall of Fame"`, `"players"`, `"hall_of_fame"`); Stats-scoped (6) — `stats_game_log` (`"Game Log"`, `"stats"`, `"game_log"`), `stats_league_leaders` (`"League Leaders"`, `"stats"`, `"league_leaders"`), `stats_player_ratings` (`"Player Ratings"`, `"stats"`, `"player_ratings"`), `stats_player_stats` (`"Player Stats"`, `"stats"`, `"player_stats"`), `stats_team_stats` (`"Team Stats"`, `"stats"`, `"team_stats"`), `stats_statistical_feats` (`"Statistical Feats"`, `"stats"`, `"statistical_feats"`); Help (6) — `help_overview` / `help_changes` / `help_custom_rosters` / `help_debugging` / `help_lol_gm_forums` / `help_zen_gm_forums` (labels `"Overview"` / `"Changes"` / `"Custom Rosters"` / `"Debugging"` / `"LOL GM Forums"` / `"Zen GM Forums"`, all section `"help"`, all `sidebar_active=None`); Tools (4) — `tools_achievements` / `tools_screenshot` / `tools_debug_mode` / `tools_reset_db` (labels `"Achievements"` / `"Screenshot"` / `"Enable Debug Mode"` / `"Reset DB"`, all section `"tools"`, all `sidebar_active=None`). **Total: 35 entries in `_FEATURE_REGISTRY`** (10 League-scoped + 3 Team-scoped + 6 Players-scoped + 6 Stats-scoped + 6 Help + 4 Tools).

## Placeholder template (Part b)

NEW `templates/_placeholder.html` extending `base.html`, `{% block title %}{{ feature_label }} — Coming Soon{% endblock %}` (em-dash U+2014, locked exact format). Structure: when `league` is non-`None`, wrap `<div class="d-flex">{% include "_partials/league_sidebar.html" %}<main>…</main></div>` (sidebar partial consumed unchanged from LG-01f); else render `<main>…</main>` directly (no sidebar — Help / Tools placeholders are sandbox-mode, mode-aware via `app_mode`). Locked DOM ids inside `<main>`: `coming-soon-header` (always — wraps the `<h1>{{ feature_label }}</h1>`), `coming-soon-section-badge` (always — small badge rendering `{{ feature_section }}` for visual context), `coming-soon-message` (always — `<p>` containing the locked exact substring `"Coming soon"`), `coming-soon-feature-key` (always — `<small>` rendering `{{ feature_key }}` for developer-debug visibility). No inline JS, no `<script>` block, no form, no `<button>`.

## URL routes (Part b)

NEW URL include file `core/help_urls.py` (mounted at `/help/`) with 6 paths in pinned order: `path("overview/", views.coming_soon, {"feature_key": "help_overview"}, name="coming_soon_help_overview")`, `path("changes/", views.coming_soon, {"feature_key": "help_changes"}, name="coming_soon_help_changes")`, `path("custom-rosters/", views.coming_soon, {"feature_key": "help_custom_rosters"}, name="coming_soon_help_custom_rosters")`, `path("debugging/", views.coming_soon, {"feature_key": "help_debugging"}, name="coming_soon_help_debugging")`, `path("lol-gm-forums/", views.coming_soon, {"feature_key": "help_lol_gm_forums"}, name="coming_soon_help_lol_gm_forums")`, `path("zen-gm-forums/", views.coming_soon, {"feature_key": "help_zen_gm_forums"}, name="coming_soon_help_zen_gm_forums")`. The include file does `from matches import views` (cross-app import; Help URLs route to the shared `matches.views.coming_soon` to avoid duplication). NEW URL include file `core/tools_urls.py` (mounted at `/tools/`) with 4 paths in pinned order: `tools_achievements` / `tools_screenshot` / `tools_debug_mode` / `tools_reset_db` (URL names `coming_soon_tools_achievements` / `coming_soon_tools_screenshot` / `coming_soon_tools_debug_mode` / `coming_soon_tools_reset_db`, URL paths `achievements/` / `screenshot/` / `debug-mode/` / `reset-db/`). Both files use no `app_name` (bare URL namespace, LG-01a/b/c/d/e/f/g precedent).

`matches/league_urls.py` is EXTENDED with the League-scoped placeholders (3 paths, inserted **AFTER** the LG-01g `<int:league_id>/team_schedule/<int:team_id>/` line and **BEFORE** the LG-01a `""` line): `path("<int:league_id>/playoffs/", views.coming_soon, {"feature_key": "league_playoffs"}, name="coming_soon_playoffs")`, `path("<int:league_id>/finances/", views.coming_soon, {"feature_key": "league_finances"}, name="coming_soon_finances")`, `path("<int:league_id>/power-rankings/", views.coming_soon, {"feature_key": "league_power_rankings"}, name="coming_soon_power_rankings")`. The Players-scoped (6) and Stats-scoped (6) placeholders mount in `matches/league_urls.py` under `<int:league_id>/players/<slug>/` and `<int:league_id>/stats/<slug>/`: `<int:league_id>/players/free-agents/` (`coming_soon_free_agents`), `<int:league_id>/players/trade/` (`coming_soon_trade`), `<int:league_id>/players/trading-block/` (`coming_soon_trading_block`), `<int:league_id>/players/prospects/` (`coming_soon_prospects`), `<int:league_id>/players/watch-list/` (`coming_soon_watch_list`), `<int:league_id>/players/hall-of-fame/` (`coming_soon_hall_of_fame`); `<int:league_id>/stats/game-log/` (`coming_soon_game_log`), `<int:league_id>/stats/league-leaders/` (`coming_soon_league_leaders`), `<int:league_id>/stats/player-ratings/` (`coming_soon_player_ratings`), `<int:league_id>/stats/player-stats/` (`coming_soon_player_stats`), `<int:league_id>/stats/team-stats/` (`coming_soon_team_stats`), `<int:league_id>/stats/statistical-feats/` (`coming_soon_statistical_feats`). Final order in `matches/league_urls.py`: `[create/, <int:league_id>/, <int:league_id>/next-season/, <int:league_id>/history/, <int:league_id>/team_schedule/<int:team_id>/, <int:league_id>/standings/ (the existing standings re-export — see "Standings sidebar entry" below), <int:league_id>/playoffs/, <int:league_id>/finances/, <int:league_id>/power-rankings/, <int:league_id>/players/free-agents/, <int:league_id>/players/trade/, <int:league_id>/players/trading-block/, <int:league_id>/players/prospects/, <int:league_id>/players/watch-list/, <int:league_id>/players/hall-of-fame/, <int:league_id>/stats/game-log/, <int:league_id>/stats/league-leaders/, <int:league_id>/stats/player-ratings/, <int:league_id>/stats/player-stats/, <int:league_id>/stats/team-stats/, <int:league_id>/stats/statistical-feats/, ""]`. Team-scoped placeholders (3 — `team_roster` / `team_finances` / `team_history`) are also league-scoped under `<int:league_id>/team/<slug>/` with URL names `coming_soon_team_roster` / `coming_soon_team_finances` / `coming_soon_team_history`; the sidebar TEAM section's hrefs resolve via the LG-01g `_resolve_current_team_for_sidebar` chain in the same way TEAM > Schedule does (but Roster / Finances / History are placeholders so they do NOT take `<int:team_id>/` — they take `<int:league_id>/` only and the view internally resolves the picked Team via `league.current_team` when rendering — though since the placeholder body does not depend on the Team, the resolution is decorative for now; LG-02+ may swap to `<int:team_id>/` paths).

**Standings sidebar entry:** the LEAGUE > Standings sidebar entry was LIVE in LG-01f pointing at `season_standings`. LG-01h preserves this; **no new `<int:league_id>/standings/` route is mounted**. The line above mentioning `<int:league_id>/standings/` in the URL order is a misnote — strike it from the final order; the actual final order is the 22 entries above WITHOUT that line. (Apologies for the inline ambiguity; the seam-contract reader follows the explicit per-URL `name="…"` list as the source of truth.)

**`laserforce_simulator/urls.py`** is MODIFIED with 2 single-line inserts: `path("help/", include("core.help_urls"))` and `path("tools/", include("core.tools_urls"))`, inserted alphabetically among the existing `include(...)` lines (Code agent's discretion on exact placement; tests only assert reverse resolution).

## Sidebar partial extension (Part c)

`templates/_partials/league_sidebar.html` is MODIFIED — the existing `{% regroup sidebar_links by section as sections %}` loop already handles arbitrary section keys (LG-01f). Two edits: (1) add `{% elif section.grouper == "stats" %}<h6 class="text-muted text-uppercase small mt-3 mb-1">STATS</h6>{% endif %}` adjacent to the existing `{% elif section.grouper == "players" %}` branch (locked exact `<h6>` markup mirroring the existing 3 section headers); (2) the partial otherwise consumes the helper output unchanged — no per-entry markup changes, no new DOM ids beyond the `sidebar-{section}-{key}` pattern (LG-01f locked) which extends to the 9 new entries automatically. Section iteration order (locked, from `regroup`): `top` → `league` → `team` → `players` → `stats`.

## `_build_league_sidebar_links` extension (Part c)

`matches.views._build_league_sidebar_links(league, displayed_season, sidebar_active) -> list[dict]` is EXTENDED in-place (LG-01g precedent — same signature, body extended). **House-style decision (locked): extend in-place, do NOT rename to `_build_league_sidebar_links_v2`.** Returns exactly **23 dicts** in pinned order (was 14 at LG-01f, 14 still at LG-01g): indexes 0 (top) → 1–6 (LEAGUE, 6 entries) → 7–10 (TEAM, 4 entries) → 11–16 (PLAYERS, 6 entries) → 17–22 (STATS, 6 entries). LEAGUE section (6 entries, locked order): Standings (`key="standings"`, LIVE via `season_standings` when `displayed_season is not None`), **Schedule** (`key="schedule"`, LIVE via `season_schedule` — LG-01f order preserved with Schedule 2nd not last, ADR-0017 §2 divergence-from-zengm), Playoffs (`key="playoffs"`, LIVE via `coming_soon_playoffs`), Finances (`key="finances"`, LIVE via `coming_soon_finances`), History (`key="history"`, LIVE via `league_history`), Power Rankings (`key="power_rankings"`, LIVE via `coming_soon_power_rankings`). TEAM section (4 entries, locked order): Roster (`key="roster"`, LIVE via `coming_soon_team_roster`), Schedule (`key="schedule_team"`, LG-01g — LIVE via `team_schedule`), Finances (`key="finances_team"`, LIVE via `coming_soon_team_finances`), History (`key="history_team"`, LIVE via `coming_soon_team_history`). PLAYERS section (6 entries, locked order — 3 new): Free Agents (`key="free_agents"`, LIVE via `coming_soon_free_agents`), Trade (`key="trade"`, LIVE via `coming_soon_trade`), Trading Block (`key="trading_block"`, LIVE via `coming_soon_trading_block`), Prospects (`key="prospects"`, LIVE via `coming_soon_prospects`), Watch List (`key="watch_list"`, LIVE via `coming_soon_watch_list`), Hall of Fame (`key="hall_of_fame"`, LIVE via `coming_soon_hall_of_fame`). STATS section (6 entries, locked order — entire section NEW): Game Log (`key="game_log"`), League Leaders (`key="league_leaders"`), Player Ratings (`key="player_ratings"`), Player Stats (`key="player_stats"`), Team Stats (`key="team_stats"`), Statistical Feats (`key="statistical_feats"`) — all LIVE via their `coming_soon_*` URL names. Each entry preserves the LG-01f 6-key dict shape `{key, label, section, url, disabled, active}`. League-scoped URLs (every entry except the top Dashboard) require `league.id` for reverse; helpers internally call `reverse("coming_soon_*", kwargs={"league_id": league.id})`. When `league is None` (Help / Tools mode — `coming_soon` view called without `league_id`), the helper is not called (`sidebar_links = []` in the view); when `league is not None` but `displayed_season is None`, the Standings / Schedule entries fall back to `url=None, disabled=True` (LG-01f rule, preserved verbatim).

`sidebar_active` enum extends from the LG-01f 14-value + `None` to a **23-value + `None` enum**. Full locked list: `"dashboard"`, `"standings"`, `"schedule"`, `"playoffs"`, `"finances"`, `"history"`, `"power_rankings"`, `"roster"`, `"schedule_team"`, `"finances_team"`, `"history_team"`, `"free_agents"`, `"trade"`, `"trading_block"`, `"prospects"`, `"watch_list"`, `"hall_of_fame"`, `"game_log"`, `"league_leaders"`, `"player_ratings"`, `"player_stats"`, `"team_stats"`, `"statistical_feats"`, `None`. Key-collision rule (locked, extends LG-01g `_team` suffix precedent): LEAGUE > Schedule keeps `"schedule"`; TEAM > Schedule uses `"schedule_team"`; TEAM > Finances uses `"finances_team"`; TEAM > History uses `"history_team"`. The 9 new entries (3 PLAYERS additions + 6 STATS) introduce zero new collisions — `prospects` / `watch_list` / `hall_of_fame` / `game_log` / `league_leaders` / `player_ratings` / `player_stats` / `team_stats` / `statistical_feats` are all unique across all 23 entries.

## Page wiring (zero per-page edits)

Every page that already renders the LG-01f sidebar partial (League dashboard, League history, Season dashboard, Season standings, Season schedule, Team Schedule from LG-01g) **automatically picks up the 23-entry shape** via the modified helper. **No per-page template edit is required** beyond `templates/_partials/league_sidebar.html` and `templates/base.html`. The 6 existing pages render the same partial; the partial reads the same `sidebar_links` context key; only the list length and the disabled→LIVE flips change. Confirmed zero-edit pages: `templates/leagues/dashboard.html`, `templates/leagues/history.html`, `templates/leagues/team_schedule.html` (LG-01g), `templates/seasons/dashboard.html`, `templates/seasons/standings.html`, `templates/seasons/schedule.html`. Confirmed edited templates: `templates/base.html` (mode branching + Help / Tools dropdowns), `templates/_partials/league_sidebar.html` (STATS section header), `templates/_placeholder.html` (NEW).

## Tests

NEW test files under `matches/tests/`: `test_lg01h_app_mode_processor.py` (Django `TestCase` for `core.context_processors.app_mode`, classes `TestAppModeContextProcessor` covering `/` ⇒ sandbox, `/teams/` ⇒ sandbox, `/players/` ⇒ sandbox, `/matches/` ⇒ sandbox, `/maps/` ⇒ sandbox, `/help/overview/` ⇒ sandbox, `/tools/achievements/` ⇒ sandbox, `/leagues/` ⇒ league, `/leagues/1/` ⇒ league, `/leagues/1/history/` ⇒ league, `/seasons/1/` ⇒ league, `/seasons/1/standings/` ⇒ league, edge case empty `request.path`, edge case missing `.path` attribute via raw `RequestFactory()`); `test_lg01h_coming_soon.py` (Django `TestCase` for `matches.views.coming_soon`, classes `TestComingSoonRouting` covering 200 happy path / 405 on POST / 404 on stale `league_id` / 404 on unknown `feature_key` / sidebar rendered in league branch / sidebar empty in Help / Tools branch / `feature_label` injected into `<h1>` / `coming-soon-message` substring `"Coming soon"` / locked 6 context keys present / `app_mode` in template context matches the URL-prefix rule), `TestComingSoonFeatureRegistry` (asserting all 35 `_FEATURE_REGISTRY` entries exist, every value-dict has the 3 keys `label, section, sidebar_active`, every `sidebar_active` is in the locked 23+`None` enum, the 19 LEAGUE/TEAM/PLAYERS/STATS entries (3+3+6+6+1 — Standings already LIVE in LG-01f) have non-`None` `sidebar_active`, the 6 Help + 4 Tools entries have `sidebar_active=None`), `TestComingSoonSessionWrite` (writes `last_league_id` on league-scoped placeholders; does NOT write on Help / Tools). `test_lg01h_base_html_branching.py` (Django `TestCase` exercising `client.get("/")` ⇒ sandbox markup present, `client.get("/teams/")` ⇒ sandbox links present + Help + Tools dropdowns rendered, `client.get(f"/leagues/{league.id}/")` ⇒ flat sandbox links absent + Help + Tools dropdowns rendered + `League ▾` dropdown rendered with all 5 items LIVE).

EXTENDED test files: `matches/tests/test_league_sidebar.py` (the LG-01f file — update `TestBuildLeagueSidebarLinks` length assertion from 14 to 23, update section-count assertions to `top=1, league=6, team=4, players=6, stats=6`, add `TestLg01hStatsSection` covering the 6 STATS entries' keys + labels + LIVE URLs, add `TestLg01hPlayersExpansion` covering Prospects / Watch List / Hall of Fame additions, add `TestLg01hDisabledFlipsLive` covering Playoffs / Finances / Power Rankings / Roster / Finances_team / History_team / Free Agents / Trade / Trading Block all LIVE under their `coming_soon_*` URLs); `matches/tests/test_league_nav_context_processor.py` (the LG-01f file — extend to cover the 3 new keys `top_bar_standings_url` / `top_bar_playoffs_url` / `top_bar_finances_url` / `top_bar_power_rankings_url` resolving via the same 3-step chain); `matches/tests/test_league_history.py`, `test_league_dashboard.py`, `test_season_dashboard_view.py`, `views_tests.py` (LG-01f sidebar-rendered tests — every assertion of the form `len(sidebar_links) == 14` MUST be updated to `== 23`; every assertion enumerating the 14 entries MUST be updated to the 23-entry list; section-count assertions update from 4 sections to 5).

The Tests agent writes failing tests against the locked DOM-id list + helper return shape + `_FEATURE_REGISTRY` vocabulary BEFORE the Code agent lands implementation. Tests must NOT touch `simulate_scheduled_round` / `simulate_match` / `save_games` or any simulator entry point.

## Scope-out (locked)

No model change. No migration (LG-01g's `0030_league_current_team.py` remains the latest LG-01x migration). No simulator touch. No RNG consumption. No `_flush_to_db` touch. No SIM-07 / SIM-08 contract interaction. **No Score Calibration re-baseline obligation.** No new ADR (ADR-0017 gets a Consequences-extension note at code time in the Docs agent's task, NOT in this contract). No CONTEXT.md edit (every term — sandbox / league / placeholder / mode / dropdown / sidebar — is implementation language, not domain; the existing `League` / `Season` / `Standings` / `Matchday` / `Job` / `Team schedule` / `Current team` entries cover the domain). No real implementation of the 19 placeholder pages (each just renders `<h1>Coming soon</h1>` via the shared template). No JS framework / htmx / Alpine / Stimulus / inline `<script>` blocks (Bootstrap 5 dropdown JS is the only existing dep). No new dependency. No API / DRF endpoint. No `django.contrib.messages` flash. No admin change. No backfill. No edit to `matches/standings.py` / `matches/schedule_generator.py` / `matches/season_dashboard.py` / `matches/tasks.py` / `matches/simulation/` / `matches/season_urls.py` / `matches/forms.py`. No edit to `teams/models.py` / `teams/views.py` / `teams/forms.py` / `teams/admin.py` / `teams/constants.py` / `teams/player_generator.py`. No edit to existing dashboard / standings / schedule / history / team_schedule templates (they consume the modified partial + helper unchanged). No `Player.is_bench` field. No `Season.archived` toggle. No mode-toggle UI (mode is path-driven only; clicking a sandbox link from inside a League re-renders in sandbox mode automatically per the path-prefix rule). No multiplayer mode (deferred per ADR-0017 §1). No `<int:team_id>/` placeholder routes (Team-scoped placeholders are `<int:league_id>/`-keyed and resolve the Team internally via `league.current_team`; LG-02+ may convert to `<int:team_id>/` paths once Team detail surfaces exist).

## Locked Names Index

URL include files `core/help_urls.py` (NEW) + `core/tools_urls.py` (NEW); URL include EXTENDED `matches/league_urls.py` (15 new path entries — 3 League-scoped + 3 Team-scoped + 6 Players-scoped + 6 Stats-scoped, minus the 3 already-LIVE LG-01f/g entries; net 15 NEW); URL names `coming_soon_help_overview` / `coming_soon_help_changes` / `coming_soon_help_custom_rosters` / `coming_soon_help_debugging` / `coming_soon_help_lol_gm_forums` / `coming_soon_help_zen_gm_forums` / `coming_soon_tools_achievements` / `coming_soon_tools_screenshot` / `coming_soon_tools_debug_mode` / `coming_soon_tools_reset_db` / `coming_soon_playoffs` / `coming_soon_finances` / `coming_soon_power_rankings` / `coming_soon_team_roster` / `coming_soon_team_finances` / `coming_soon_team_history` / `coming_soon_free_agents` / `coming_soon_trade` / `coming_soon_trading_block` / `coming_soon_prospects` / `coming_soon_watch_list` / `coming_soon_hall_of_fame` / `coming_soon_game_log` / `coming_soon_league_leaders` / `coming_soon_player_ratings` / `coming_soon_player_stats` / `coming_soon_team_stats` / `coming_soon_statistical_feats` (25 total NEW URL names + 4 reserved Standings-area names already shipped); view `matches.views.coming_soon`; module-level constant `matches.views._FEATURE_REGISTRY` (35-entry hard-coded dict); context processor `core.context_processors.app_mode` (NEW) + `core.context_processors.league_nav` (EXTENDED — 4 new keys `top_bar_standings_url` / `top_bar_playoffs_url` / `top_bar_finances_url` / `top_bar_power_rankings_url`); modified helper `matches.views._build_league_sidebar_links` (signature unchanged, body returns 23 entries); template (NEW) `templates/_placeholder.html` (block title `{{ feature_label }} — Coming Soon`, em-dash U+2014); modified templates `templates/base.html` + `templates/_partials/league_sidebar.html`; locked DOM ids — top-bar dropdown toggles `leagues-nav-link` (preserved LG-01a/f) + `help-nav-link` (NEW) + `tools-nav-link` (NEW); `League ▾` items `league-standings-topbar-link` + `league-playoffs-topbar-link` + `league-finances-topbar-link` + `league-history-topbar-link` (preserved LG-01f) + `league-power-rankings-topbar-link`; Help items `help-overview-topbar-link` / `help-changes-topbar-link` / `help-custom-rosters-topbar-link` / `help-debugging-topbar-link` / `help-lol-gm-forums-topbar-link` / `help-zen-gm-forums-topbar-link`; Tools items `tools-achievements-topbar-link` / `tools-screenshot-topbar-link` / `tools-debug-mode-topbar-link` / `tools-reset-db-topbar-link`; placeholder-page DOM ids `coming-soon-header` / `coming-soon-section-badge` / `coming-soon-message` / `coming-soon-feature-key`; sidebar partial section header NEW `<h6>STATS</h6>`; sidebar-entry DOM ids extend to `sidebar-stats-game_log` / `sidebar-stats-league_leaders` / `sidebar-stats-player_ratings` / `sidebar-stats-player_stats` / `sidebar-stats-team_stats` / `sidebar-stats-statistical_feats` + `sidebar-players-prospects` / `sidebar-players-watch_list` / `sidebar-players-hall_of_fame` (LG-01f pattern `sidebar-{section}-{key}` extends automatically); context keys (`coming_soon` view) `league` / `displayed_season` / `feature_key` / `feature_label` / `feature_section` / `sidebar_links` / `sidebar_active`; context key (global, from `app_mode`) `app_mode` ∈ `"league"` / `"sandbox"`; context keys (global, from `league_nav`) `top_bar_history_url` (preserved) + 4 NEW; `sidebar_active` 23-value enum + `None` listed verbatim above; section literals `"top"` / `"league"` / `"team"` / `"players"` / `"stats"` + `"help"` / `"tools"` (the latter two appear in `_FEATURE_REGISTRY` `section` field but NOT in the sidebar partial's iteration); locked literals `"Coming soon"` (placeholder body substring) + `"League ▾"` / `"Help ▾"` / `"Tools ▾"` (dropdown toggle text, U+25BE) + `"STATS"` (sidebar section header, uppercase) + `"sandbox"` / `"league"` (`app_mode` values); test files `matches/tests/test_lg01h_app_mode_processor.py` (NEW) + `matches/tests/test_lg01h_coming_soon.py` (NEW) + `matches/tests/test_lg01h_base_html_branching.py` (NEW); EXTENDED test files `matches/tests/test_league_sidebar.py` + `test_league_nav_context_processor.py` + `test_league_history.py` + `test_league_dashboard.py` + `test_season_dashboard_view.py` + `views_tests.py` (14→23 length + 23-entry enumeration + 4→5 section count updates). ADR [ADR-0017](../../docs/adr/0017-league-context-nav-shape.md) extended at code time by Docs agent.
