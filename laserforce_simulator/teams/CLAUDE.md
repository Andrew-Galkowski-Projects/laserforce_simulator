# teams/

Manages teams, players, and rosters. Serves as the homepage (`/`).

## Models (`teams/models.py`)

**`Team`**: Has exactly 6 `Player` slots — one each of Commander, Heavy, Scout, Medic, Ammo, plus one duplicate role.

**`Player`**: Belongs to a team and has an assigned role. Carries 19 numeric stats (0–100) used as weights by the simulator:

| Category | Fields |
|----------|--------|
| Awareness | `player_awareness`, `game_awareness`, `resource_awareness` |
| Decision-making | `decision_making` |
| Physical | `positioning`, `stamina`, `speed`, `flexibility`, `adaptability` |
| Team | `communication`, `teamwork` |
| Role | `Offensive_synergy`, `defensive_synergy`, `midfield_synergy`, `resupply_synergy`, `resupply_efficiency`, `accuracy`, `survival`, `special_usage` |

`overall_rating` is a `@property` returning the unweighted mean of all 19 stats.

As of **LG-04 player development**, the 19 stat fields are **mutated in place** at each League
`next_season` rollover (a ZenGM-style age curve) and `total_games` is **ticked** there too —
cosmetically: an active-roster player by their real regular-season appearances, a free-agent by
a small random amount (`total_games` is never a develop input). A per-Season immutable snapshot
of the 19 stats + age + overall is persisted as `matches.PlayerSeasonRating`. See the
[`matches/CLAUDE.md`](../matches/CLAUDE.md) **LG-04 player development + ratings history** section
for the full mechanism.

As of **LG-05 player potential**, `Player.potential` (a nullable `FloatField`, `default=None`;
migration `0012_player_potential.py`) holds a per-Player **projected peak overall** — set at each
League `next_season` rollover (and the founding `league_create` baseline) by LG-05 alongside
development, **mutated in place** like the 19 stats. It is `None`/`default=None` for players outside
any league flow. Details in [`matches/CLAUDE.md`](../matches/CLAUDE.md) **LG-05 player potential**.

As of **FIN-01 team finance subsystem**, `Player.salary` (a nullable `FloatField`, `default=None`;
migration `0013_player_salary_team_finance.py`) holds a **derived salary** — a deterministic
cap-scaled function of `overall_rating` (`matches.finance.salary_for_overall`), recomputed in place
at the same write sites LG-05 recomputes `potential` (`_write_baseline_ratings` +
`_develop_league_for_new_season`), but **only when the League has `finance_enabled`** — otherwise it
stays `None` (byte-identical to pre-FIN-01). It is **finance-only** (a payroll input — the sum of a
Team's 6 active-roster salaries — for the owner-mood money axis, never a sim input, never edited via
a form) and **derived, not contracted** (no free agency / cap space). `None` for players outside any
finance-enabled flow. Details in [`matches/CLAUDE.md`](../matches/CLAUDE.md) **FIN-01 team finance
subsystem**.

`stat_for_simulation(stat_name, role)` returns `min(int(raw_value * 1.2), 100)` when `role in self.preferred_roles`, otherwise the raw stat value. Invalid `stat_name` values raise `AttributeError` naturally (no explicit guard). Used by `PlayerRoundState` forwarding properties and `BatchSimulator._make_players` to apply the preferred-role boost at simulation time without affecting stored values or `overall_rating`.

`PlayerForm` exposes all 19 stat fields (default 50) with "Set All to Average (50)" and "Set All to Elite (90)" preset buttons. Profile fields (age, started_playing_age, total_games, home_site, height) are also on the form; when adding a new player, the view pre-fills them via `_random_player_profile()`.

`_random_player_profile()` (in `models.py`) returns a dict of randomised profile values drawn from `teams/constants.py`. Age is 16–50; started_playing_age is 16–age; total_games is 0–5000; height is 4'0"–6'10"; home_site is drawn from `LASERFORCE_SITES`; name is drawn from `PLAYER_NAMES`.

`ROLE_STATS` is imported from `matches.sim_helpers.role_constants` — the canonical source for all role-level constants (`ROLE_STATS`, `MAX_LIVES`, `MAX_SHOTS`, `SPECIAL_COST`). Both `teams/models.py` and `sim_helpers/player_state.py` import from there; the duplicate definition that previously lived in `player_state.py` has been removed.

### Team.is_draw_team

`Team.is_draw_team` (`BooleanField(default=False)`, added by **LG-02x-1**) marks a Team that was assembled by a Random Draw Tournament's tier-balanced draw rather than hand-built. It carries **NO FK to the Tournament / Match** — the durable link from a drawn Team back to its Tournament and its drawn players lives on `matches.TournamentPlayerEntry` (avoiding a `teams → matches` dependency inversion). A drawn Team's `slot_*` FKs hold only the **transient** per-Round role assignment the simulator's `before_round_hook` rewrites in memory each Round; the source of truth for `(player, tier, drawn_team)` is `TournamentPlayerEntry`. Existing rows take `default=False` (no backfill; ADR-0004 disposable-data precedent).

**`Team.roster_errors` relaxation (draw teams only).** Because a drawn Team **references borrowed Players** without `Player.team` ever being reassigned by the draw, the "all players belong to this team" ownership check is **skipped for draw teams**: the `for player, role, slot_name in filled:` belongs-to-team loop (the `player.team_id != self.pk` check) is wrapped in `if not self.is_draw_team:`. **All other validations stay enforced for draw teams** — the all-6-slots-filled check, the **duplicate-player** check, and the **role-distribution** (Scout-only-twice) check. A non-draw Team with a foreign player **still** errors. See [`matches/CLAUDE.md`](../matches/CLAUDE.md) `## LG-02x-1 random draw player-pool tournament` and [ADR-0022](../../docs/adr/0022-random-draw-player-pool-tournament.md).

### Team finance fields (FIN-01)

**FIN-01 team finance subsystem** adds five `Team` finance fields (migration
`0013_player_salary_team_finance.py`, all neutral defaults so non-finance Teams are unaffected):
`budget_scouting` / `budget_coaching` / `budget_facilities` (`PositiveSmallIntegerField(default=34)`)
are **ZenGM 1–100 budget levels** (`matches.finance.DEFAULT_LEVEL = 34` is the neutral level —
mid-range cost, zero effect) converted to dollars via `finance.level_to_amount`; `ticket_price` /
`cash` (`FloatField(default=0.0)`) are dollar amounts (`cash` accumulates each Season's profit and
carries across the rollover). **No choices, no validators** — the per-League `finance_enabled` toggle
+ AI defaults keep the levels in band and the Team Finances form clamps user input to `[1, MAX_LEVEL]`.
**Cost-only this slice:** all three budgets are pure expense line-items feeding season profit, and
facilities additionally feeds the revenue/attendance side; scouting & coaching buy **no gameplay edge
yet** (FIN-02 wires coaching into LG-04 development, FIN-03 wires scouting into LG-05 potential).
Details in [`matches/CLAUDE.md`](../matches/CLAUDE.md) **FIN-01 team finance subsystem** and
[ADR-0027](../../docs/adr/0027-team-finance-subsystem.md).

## Constants (`teams/constants.py`)

Static name pools used by `_random_player_profile()`:

- `PLAYER_NAMES` — ~386 laser-tag codenames drawn from real venue scorecards.
- `LASERFORCE_SITES` — 12 real Laserforce venue locations.

These are imported into `models.py` and re-exported so existing code that does `from teams.models import PLAYER_NAMES` continues to work.

## REST API (`teams/serializers.py`, `teams/api_views.py`)

Read-only DRF endpoints registered under `/api/`:

| Endpoint | Serializer | Notes |
|----------|-----------|-------|
| `GET /api/teams/` | `TeamListSerializer` | Slim — nested players include id/name/preferred_roles only |
| `GET /api/teams/<id>/` | `TeamSerializer` | Full — nested players include all 19 stats |
| `GET /api/players/` | `PlayerSerializer` | Paginated, ordered by team then name |
| `GET /api/players/<id>/` | `PlayerSerializer` | Full player detail |

**Serializer split:** `TeamListSerializer` (list) nests `PlayerInlineSerializer` (id, name, preferred_roles) to keep list payloads small. `TeamSerializer` (detail) nests the full `PlayerSerializer` with explicit stat fields. Both share `_TEAM_BASE_FIELDS` and `_PLAYER_STAT_FIELDS` constants so the field lists are defined once.

**`PlayerInlineSerializer`** — minimal player representation (id, name, preferred_roles) for use anywhere a full player is not needed.

**`PlayerSerializer`** — all 19 stats; explicit field list guarding against accidental exposure of future model fields.

## URLs

```
/           → team list (homepage)
/teams/     → team CRUD, player management

/api/teams/         → TeamViewSet (list, detail)
/api/players/       → PlayerViewSet (list, detail)
```

## HX-01 career stats

A per-player career page aggregating `PlayerRoundState` across every round the player appears in, served at `GET /players/<int:player_id>/stats/` (URL name `player_career_stats`, view `teams/views.py::player_career_stats`). The URL deliberately sits at the flat `/players/<pid>/` root — **not** nested under `/teams/<id>/` — so a future cross-team-history feature does not need to break URLs even though every `Player.team` FK is single-CASCADE today.

**URL include.** A NEW URL file `teams/player_urls.py` (`app_name = None` — explicit; reverse stays the bare `'player_career_stats'`, no namespace prefix) is included from `laserforce_simulator/urls.py` as `path("players/", include("teams.player_urls"))` placed **above** the `path("", include("teams.urls"))` homepage catch-all. **Order matters** — Django resolves top-to-bottom, so the include must sit above the `""` catch-all or the homepage will shadow it.

**Pure aggregation module.** `teams/career_stats.py` is the algorithmic seam — **pure Python, no Django imports, no ORM, no RNG, no I/O** (frozen import allowlist: `typing`, `collections`, optional `math`, and `SPECIAL_COST` from `matches.sim_helpers.role_constants`). The Tests agent pins this with a "no Django imports leaked" defensive check mirroring the RES-04 / RV-03 precedent. The module's public surface is four functions:

- `summarize(rounds: Iterable[Mapping]) -> dict` — career totals across every round, returning **exactly** six keys `{games, avg_points, tag_ratio, avg_survival_ticks, avg_accuracy_pct, avg_sp_earned}` (empty input ⇒ `games=0` and every other key `0.0`, no division by zero).
- `summarize_by_role(rounds) -> list[dict]` — per-role breakdown, one entry per role **actually played**, in the locked order Commander/Heavy/Scout/Medic/Ammo (roles not played are omitted; empty input ⇒ `[]`).
- `points_trend(rounds, window=10) -> list[list]` — rolling-mean trend of `points_scored`, returning `[[round_idx, mean_points], …]` with `round_idx` 1-based, sorted ascending by `(date_played, game_round_id)` tiebreaker, partial trailing window for rounds 1..9 and full 10-window for rounds 10+. The `list[list]` (not `list[tuple]`) shape makes `json_script` serialisation trivial.
- `rolling_mean(values: list[float], window=10) -> list[float]` — the pure helper used internally by `points_trend`, exported so tests can pin it directly without depending on `points_trend` ordering.

**Formulas (frozen, sum/sum where statistically required).** `Tag ratio` = `sum(tags_made) / max(sum(times_tagged), 1)` — sum/sum, **not** mean-of-per-round-ratios; pinned by `test_tag_ratio_is_sum_over_sum_not_mean_of_ratios` against the deliberately-asymmetric `10/1` vs `0/100` two-round case where mean-of-ratios would yield `5.0` and sum/sum yields ≈ `0.099`. `Avg survival ticks` = `mean(min(was_eliminated_at, 1800))` — the cap is TIME-01's `TICKS_PER_ROUND = 1800`, so `SURVIVED_SENTINEL = 1801` contributes 1800 (the `÷2` tick → second conversion is applied at the **template** layer only via the existing `team_extras.div` filter, TIME-01). `Avg accuracy` = `sum(tags_made) / max(sum(tags_made + shots_missed), 1) × 100`. `Avg SP earned` = `mean(final_special + SPECIAL_COST.get(role, 0) × specials_used)` — the `.get` fallback contributes **0** for Heavy (which has no `SPECIAL_COST` entry), pinned by `test_avg_sp_earned_mixed_roles_includes_heavy_fallback`.

**Round-dict crossing the view ↔ pure-module seam.** The view assembles a list of plain-dict rounds and hands the list to the pure functions; each dict carries **exactly** ten frozen keys `{role, points_scored, tags_made, times_tagged, shots_missed, final_special, specials_used, was_eliminated_at, date_played, game_round_id}` — no extras, no aliases. The pure module never sees a Django model, `PlayerRoundState`, `GameRound`, `select_related`, or any ORM type.

**View.** `player_career_stats(request, player_id)` runs `get_object_or_404(Player, pk=player_id)` (→ 404 on missing) and **exactly one** ORM query — `PlayerRoundState.objects.filter(player=player).select_related("game_round").order_by("game_round__date_played", "game_round_id")` — then assembles the round-dict list, calls `summarize` / `summarize_by_role` / `points_trend`, and renders `templates/teams/player_career_stats.html` with **six** frozen context keys: `player`, `total_rounds`, `career`, `per_role`, `trend`, `has_rounds` (with `has_rounds = total_rounds > 0`). The view is read-only; no `@require_GET` decorator is contracted (Django views accept any method by default and the view is non-destructive).

**Template surface.** `templates/teams/player_career_stats.html` extends `base.html`, `{% load team_extras %}` for the `div` filter, and renders three surfaces gated on `has_rounds`: a 6-column career-totals row (DOM id `career-totals-table`), the per-role table (DOM id `career-per-role-table`, `|title`-cased role labels), and a Chart.js dashed-line rolling-10 trend chart (canvas DOM id `points-trend-chart`, json_script id `trend-data`, dataset label `"Avg points (rolling 10)"`, x-axis title `"Round number"`, y-axis title `"Avg points (rolling 10)"`, `pointRadius: 2`). The empty branch renders a notice (DOM id `career-no-rounds-notice`) containing the substring `"No rounds played yet"` in place of the three surfaces; tests pin via substring match. Formatting is locked: avg points `|floatformat:1`, tag ratio `|floatformat:2`, survival `|div:2|floatformat:0` + `s` suffix (the `div` filter is the same `teams/templatetags/team_extras.py::div` used elsewhere for tick → seconds at the template layer, TIME-01), accuracy `|floatformat:0` + `%`, SP earned `|floatformat:1`, role labels `|title`-cased so `"commander"` renders as `"Commander"`.

**Entry point.** A single `"Career stats"` anchor in `templates/teams/player_detail.html` reversing `{% url 'player_career_stats' player.id %}`. Tests pin via substring `"Career stats"` in the rendered `/teams/<team_id>/player/<player_id>/` response body. **HX-04 addition:** the career page itself gains an outgoing `player-h2h-link` "Head-to-head" anchor in its header (sibling to the existing `role-benchmarks-link`) reversing `{% url 'player_head_to_head' %}?player_a={{ player.id }}` — the single locked entry point for the HX-04 Player head-to-head record view (see [`matches/CLAUDE.md`](../matches/CLAUDE.md) `## HX-04 player head-to-head`).

**Determinism / scope.** **Read-only view** — no RNG, no simulation, no `_flush_to_db` touch, no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline obligation. **No model change, no migration**, no ADR (reversible: pure read-only view + pure aggregation module), no CONTEXT.md edit (the **Tag ratio** term was added inline during the grilling session that produced this contract). Tests live in a NEW `teams/tests/test_career_stats.py` — a pure-unit class for the four pure functions (empty inputs, single-round happy path, sum/sum tag-ratio direction, Heavy `SPECIAL_COST` fallback, `was_eliminated_at=1801` capping to 1800, all-misses accuracy, role ordering Commander/Heavy/Scout/Medic/Ammo, role omission, `rolling_mean` partial-then-full window, `points_trend` `(date, game_round_id)` tiebreaker, and the "no Django imports leaked" defensive check) plus a Django `TestCase` class for the view (200 with rounds + all six context keys, 200 empty state with the `"No rounds played yet"` substring, 404 on missing `player_id`, and the `"Career stats"` link rendered on `/teams/<team_id>/player/<player_id>/`). Locked names (URL, URL name, pure module, public surface, DOM ids, json_script ids, template paths, context keys) are pinned by the seam contract at [`.claude/worktrees/hx-01-seam-contract.md`](../../.claude/worktrees/hx-01-seam-contract.md).

## HX-01b career page 12-stat benchmark extension

Extends the HX-01 per-role table on `/players/<int:player_id>/stats/` from its original 5 display rows to **15 rows per role actually played** — the original 5 HX-01 display stats plus the 10 net-new HX-02 `STAT_KEYS` rows (the full 12-tuple minus the 2 already overlay-mapped via `_HX01_TO_BENCHMARK_STAT`). Layout pivot: the single wide `<table id="career-per-role-table">` is replaced with a `<section id="career-per-role-table">` wrapper (preserving the HX-01-locked DOM id) containing one `<table id="career-per-role-table-{role}">` per role the subject has played, each with 15 rows and columns `Stat | Player value | Mean | Median | Δ | Percentile | n`. Each row carries `<tr id="career-stat-row-{role}-{key}">` and benchmark cells continue using the HX-02-shipped `<span id="benchmark-{role}-{key}-{component}">` template (`component` ∈ `{mean, median, delta, percentile, n}`).

**Frozen 15-row order** (pinned by `test_stat_rows_order_is_locked`): rows 0–4 are the original 5 HX-01 display stats in their current order — `avg_points`, `tag_ratio`, `avg_survival_ticks`, `avg_accuracy_pct`, `avg_sp_earned` — followed by rows 5–14, the 10 net-new `STAT_KEYS` in declaration order skipping `points_scored` and `accuracy`: `mvp`, `tags_made`, `times_tagged`, `final_lives`, `resupplies_given`, `missiles_landed`, `specials_used`, `follow_up_shots`, `reaction_shots`, `combo_resupply_count`. Locked human labels are `Avg points / Tag ratio / Avg survival / Avg accuracy / Avg SP earned / MVP score / Tags made / Times tagged / Final lives / Resupplies given / Missiles landed / Specials used / Follow-up shots / Reaction shots / Combo resupplies`. The `{key}` field is mixed-namespace by design — HX-01 display keys for rows 0–4, raw STAT_KEYS names for rows 5–14 — and feeds the DOM id template verbatim.

**Context shape (additive).** Each `per_role_with_benchmarks[i]` row gains a `stat_rows: list[dict]` field with exactly 15 ordered entries; each entry is `{key, label, player_value: float, benchmark: dict | None}`. The `benchmark` field is `None` for the 3 HX-01-only derived rows (`tag_ratio`, `avg_survival_ticks`, `avg_sp_earned`) — they render `<td class="benchmark-na">—</td>` placeholders for all 5 benchmark cells — and is a `player_position(...)` result dict (`{benchmark_mean, benchmark_median, delta_mean, delta_median, percentile, qualified, n}`) for the 12 benchmark-backed rows. The existing `benchmarks_by_stat` dict is **preserved verbatim** for back-compat with `test_per_role_with_benchmarks_contains_benchmarks_by_stat`; only the new `stat_rows` field drives the rendered template.

**Player-value sources.** Rows 0–4 (the 5 HX-01 keys) pull `player_value` directly from the existing `summarize_by_role` row dict — no recomputation. Rows 5–14 (10 net-new) call `compute_career_stat_for_role(player_role_rounds, stat)` from `teams/role_benchmarks.py` — the same helper that already feeds the benchmark percentile path inside `_build_per_role_overlay`, so the subject value and the overlay are guaranteed identical. The frozen row spec lives at module scope in `teams/views.py::_HX01B_STAT_ROW_SPEC` (15-tuple of `(key, label, benchmark_stat | None)`), and `_build_per_role_overlay` iterates it inline.

**Empty-state UX** mirrors today's `avg_points` / `avg_accuracy_pct` cells exactly: below-threshold subject delta cells render the substring `— (need {min_rounds}+ rounds)`; empty `(role, stat)` populations render `n = 0`; HX-01-only rows always render the `benchmark-na` `—` placeholders regardless of qualification.

**Determinism / scope.** **Read-only view extension** — no RNG, no simulation, no `_flush_to_db` touch, no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline obligation. **Zero diff** to `teams/career_stats.py` (HX-01 pure module, "no new pure-module work"), `teams/role_benchmarks.py`, `teams/role_benchmarks_cache.py`, `teams/signals.py`, URL files, `matches/`; no model change, no migration, no ADR, no CONTEXT.md edit (no new domain terms — `Tag ratio` / `Role benchmark` / `Percentile rank` already defined; the 10 net-new stat names are the same `STAT_KEYS` already documented under HX-02). Tests: 6 new TestCases appended to `teams/tests/test_role_benchmarks_view.py::TestPlayerCareerStatsExtended` (`test_stat_rows_is_15_entry_ordered_list_per_role`, `test_stat_rows_order_is_locked`, `test_hx01_only_rows_carry_none_benchmark`, `test_net_new_rows_subject_value_matches_compute_career_stat_for_role`, `test_below_threshold_subject_renders_need_n_rounds_on_all_12_benchmarked_rows`, `test_per_role_table_dom_ids_present_per_role_played`). Locked names (DOM ids, the 15-entry row order, labels, formatters, the additive context field) are pinned by the seam contract at [`.claude/worktrees/hx-01b-seam-contract.md`](../../.claude/worktrees/hx-01b-seam-contract.md).

## HX-02 role benchmarks

Per-`(Role, Stat)` **Role benchmark** statistics (mean / median / p25 / p75 / p90 / n) and per-player **Percentile rank** (both terms in [CONTEXT.md](../../CONTEXT.md)) computed over the population of every player's career-average-when-playing-that-Role across all `PlayerRoundState` rows. Surfaced two ways: a standalone per-role page at `GET /players/benchmarks/` (URL name `role_benchmarks`, view `teams/views.py::role_benchmarks`) rendering one table per role across all 12 `STAT_KEYS`, **plus** an additive extension to the HX-01 per-player career page that decorates each per-role stat cell with mean/median/delta/percentile/n.

**URL include.** The new entry sits at the top of `teams/player_urls.py` — listed **FIRST**, above the existing `<int:player_id>/stats/` HX-01 route — so the `/players/benchmarks/` literal does not get shadowed by the `<int:player_id>` capture group. The HX-01 outer include (`path("players/", include("teams.player_urls"))` placed above the homepage catch-all in `laserforce_simulator/urls.py`) is reused unchanged.

**Pure aggregation module.** `teams/role_benchmarks.py` is the algorithmic seam — **pure Python, no Django imports, no ORM, no RNG, no I/O** (frozen import allowlist: `bisect`, `statistics`, `collections.defaultdict`, `typing.Iterable`/`Mapping`). The "no Django imports leaked" defensive check mirrors the HX-01 / RES-04 / RV-03 precedent. Module-level constants: `STAT_KEYS` (12-tuple in frozen order — `points_scored, tags_made, times_tagged, accuracy, shots_missed, final_special, specials_used, mvp, final_lives, resupplies_given, missiles_landed, follow_up_shots, reaction_shots, combo_resupply_count`), `RATIO_STATS = frozenset({"accuracy"})`, `MVP_DERIVED_STATS = frozenset({"mvp"})`, `ROLES = ("commander", "heavy", "scout", "medic", "ammo")`. The public surface is six functions: `build_role_populations(rounds, *, threshold=0)`, `apply_threshold(populations, *, threshold)`, `summarize_population(values)`, `percentile_for(values, x)`, `compute_role_benchmarks(rounds, *, threshold=0)`, and `player_position(populations, role, stat, x)`.

**Aggregation rule.** Per-round mean for every stat in `STAT_KEYS` **except** `accuracy`, which uses the Tag-ratio-style sum/sum shape `sum(tags_made) / max(sum(tags_made + shots_missed), 1)` per-player within that role's subset (mirroring the HX-01 `Avg accuracy` formula). The view-side pre-computed `mvp` (from `calculate_mvp`) is treated as a per-round value and rolled up with the same per-round-mean rule as every other counter — the seam carries `mvp` already-computed so the pure module never touches MVP weight logic.

**Subject-inclusion policy.** Mean / median / percentile are computed over the FULL population INCLUDING the subject, so the standalone-page row and the HX-01-overlay cell for the same player are **guaranteed identical** (no off-by-one between an "all players" framing on the standalone page and an "all other players" framing on the per-player overlay). Population maximum maps to `100.0`. Below-threshold subjects (`player_position` returns `qualified=False`) render `— (need N+ rounds)` in the view, substituting the active `threshold` value.

**Query params.** `?threshold=<int>` (default `5`, clamped `≥ 0` — negatives, non-int strings, and missing values fall back to `5`) and `?display=mean|median` (default `mean` — any other value falls back to `mean`). Threshold gates *visibility* per `(role, stat)` row, not the underlying cached samples.

**HX-01 → STAT_KEYS overlay mapping (v1).** Only `avg_points → points_scored` and `avg_accuracy_pct → accuracy` map onto a STAT_KEYS entry and so receive benchmark cells on the HX-01 page; the other three HX-01 display stats (`tag_ratio`, `avg_survival_ticks`, `avg_sp_earned`) render `<td class="benchmark-na">—</td>` until **HX-01b** lands (which extends the per-role table to the full 12-row set so every benchmarked stat surfaces its overlay). `tag_ratio` / `avg_survival_ticks` / `avg_sp_earned` remain HX-01-only derived stats — they are not in `STAT_KEYS` and HX-02 does not benchmark them.

**Round-dict crossing the view ↔ pure-module seam.** A strict **18-key SUPERSET** of HX-01's 10-key dict — additive, with HX-01 signatures unchanged: the 10 HX-01 keys (`role, points_scored, tags_made, times_tagged, shots_missed, final_special, specials_used, was_eliminated_at, date_played, game_round_id`) plus **6 HX-02 raw counters** (`final_lives, resupplies_given, missiles_landed, follow_up_shots, reaction_shots, combo_resupply_count`, all int) plus **2 view-side pre-computed floats** (`mvp` via `PlayerRoundState.get_mvp` / `calculate_mvp`, `accuracy_pct` via `PlayerRoundState.get_accuracy`). The pure module never sees a Django model — only plain dicts.

**Cache helper.** `teams/role_benchmarks_cache.py` exposes `get_all_benchmark_data(threshold)`, `get_role_benchmark_samples(role, stat)`, and `invalidate_role_benchmarks()`. Backing store is the Django cache framework keyed by a global version int: `role_benchmark_version` holds the int; `role_benchmark:v{version}:{role}:{stat}` holds per-`(role, stat)` raw samples. A **single full-table scan on miss** populates every `(role, stat)` key for all 5 roles × 12 stats in one pass — second request for any cell post-invalidation is a hit (via `cache.get_many`, one round-trip for all 65 keys). Cached samples are **threshold-independent**: `apply_threshold` runs per request against the cached raw populations, so toggling `?threshold=` does not bust the cache. Invalidation is performed exclusively via `invalidate_role_benchmarks()` (which schedules the version bump under `transaction.on_commit` so a pre-commit reader can't repopulate the cache with not-yet-committed-stale data, then bumps `role_benchmark_version`), never by deleting individual keys. **Cache-miss cost scales with total `PlayerRoundState` row count, not the requesting player** — the first `/players/<id>/stats/` or `/players/benchmarks/` request after any write triggers a full-table scan to repopulate the cache; subsequent reads under the same version are cache-only.

**Signal handler + simulator hook.** `teams/signals.py::_bump_role_benchmark_version` registers `post_save` + `post_delete` on `PlayerRoundState` and calls `invalidate_role_benchmarks()` on every save / delete. **Crucially**, `BatchSimulator._flush_to_db` writes via `bulk_create`, which **does not fire `post_save`**, so the signal alone would miss every batch-simulated round — the simulator therefore performs a one-line **lazy-import** call to `invalidate_role_benchmarks()` immediately before its final return. The lazy import inside `_flush_to_db` avoids any `teams ↔ matches` circular-import risk; this is the **only** simulator change for HX-02 and it does not touch any simulation mechanic.

**Template surface.** NEW `templates/teams/role_benchmarks.html` (DOM ids `benchmark-filter-form`, `benchmark-threshold-input`, `benchmark-display-toggle`, `benchmark-table-{role}` ×5, `benchmark-row-{role}-{stat}`, `benchmark-no-data-notice`) and EXTENDED `templates/teams/player_career_stats.html` (new cells `benchmark-{role}-{stat_key}-{mean|median|delta|percentile|n}`, class `benchmark-na` for stats not in `STAT_KEYS`, anchor `role-benchmarks-link` pointing at `/players/benchmarks/`). The standalone page emits one `<table id="benchmark-table-{role}">` per role; each row id is `benchmark-row-{role}-{stat}` and contains 6 cells (mean/median/p25/p75/p90/n). The display toggle (`?display=mean`/`median`) swaps which of mean/median sits in the leading delta-target column.

**Empty-population UX.** `n=0` cells render `—` + `n = 0` (so the substring `n = 0` is greppable for tests). Below-threshold subject cells render `— (need N+ rounds)` substituting the active `threshold`. The standalone page emits `<div id="benchmark-no-data-notice">` when **every** `(role, stat)` returns `n=0` (e.g. on a brand-new install with no rounds), suppressing the 5 empty role tables.

**Entry point.** A `role-benchmarks-link` anchor in `templates/teams/player_career_stats.html` reversing `{% url 'role_benchmarks' %}` lets a viewer of one player's stats jump to the cross-role comparison page.

**View context.** `role_benchmarks(request)` ships `{min_rounds, display, roles, benchmarks, stat_keys}`; `player_career_stats` extends its existing six HX-01 context keys additively with `{min_rounds, display, stat_keys, per_role_with_benchmarks}` (so HX-01-only template branches keep working; HX-02-aware branches read the new keys).

**Determinism / scope.** **Read-only views** — no RNG, no simulation, no `_flush_to_db` simulation change (the one-line `invalidate_role_benchmarks()` cache-bust is a cross-cutting hook, not a simulation mechanic), **no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline obligation**. **No model change, no migration, no ADR** (decisions are reversible — pure module + cache helper + read-only views + a one-line simulator cache-bust), no CONTEXT.md edit (the `Role benchmark` and `Percentile rank` terms were added inline during the grilling session that produced this contract).

Locked names — URL `GET /players/benchmarks/` (URL name `role_benchmarks`); views `role_benchmarks` + `player_career_stats`; pure module `teams/role_benchmarks.py`; cache helper `teams/role_benchmarks_cache.py`; signal handler `teams/signals.py::_bump_role_benchmark_version`; simulator hook `invalidate_role_benchmarks()` call inside `BatchSimulator._flush_to_db`; templates `templates/teams/role_benchmarks.html` (new) + `templates/teams/player_career_stats.html` (extended); DOM ids `benchmark-filter-form` / `benchmark-threshold-input` / `benchmark-display-toggle` / `benchmark-table-{role}` / `benchmark-row-{role}-{stat}` / `benchmark-no-data-notice` / `benchmark-{role}-{stat_key}-{mean|median|delta|percentile|n}` / `role-benchmarks-link`; class `benchmark-na`; test files `teams/tests/test_role_benchmarks.py` + `teams/tests/test_role_benchmarks_view.py` + `teams/tests/test_role_benchmarks_cache.py`; context keys `min_rounds, display, roles, benchmarks, stat_keys, per_role_with_benchmarks`; cache keys `role_benchmark:v{version}:{role}:{stat}` + `role_benchmark_version`; query params `?threshold=<int>` (default 5, clamp ≥ 0) + `?display=mean|median` (default `mean`). Pinned by the seam contract at [`.claude/worktrees/hx-02-seam-contract.md`](../../.claude/worktrees/hx-02-seam-contract.md).

## LG-00 generation

Bulk player/team generation surface at `GET /teams/generate/` (URL name `generate_players`, view `teams/views.py::generate_players`, form `teams/forms.py::GenerateLeagueForm`). Two output modes selected by the form's `num_teams` dropdown: `num_teams ≥ 1` creates new **Teams** with auto-filled 6-slot rosters + optional bench when `players_per_team > 6`; `num_teams == "0"` creates a flat free-agent pool of 12–100 players on the reserved **Free Agents Team** (CONTEXT.md — magic name `"Free Agents"`, no `is_system` field, no model change, no migration). The new entry in `teams/urls.py` is `path("generate/", views.generate_players, name="generate_players")` (no `app_name`, mirroring the existing HX-01 pattern — `reverse("generate_players")` resolves without a namespace prefix).

**Pure generation module.** `teams/player_generator.py` is the algorithmic seam — **pure Python, no Django imports, no ORM, no settings access, no I/O, no global RNG** (frozen import allowlist: `random` for `Random` type-hint only with the RNG **injected** by the caller, `typing` for annotations, optional `collections`). The "no Django imports leaked" defensive check mirrors the RES-04 / RV-03 / HX-01 / HX-02 / HX-03 / HX-04 precedent. The module's public surface is three functions:

- `draw_stats(rng: random.Random, mean: float, std_dev: float) -> dict[str, int]` — returns a dict with **exactly 19 keys** in the module-level `_STAT_FIELDS` canonical order (3 awareness + 1 decision + 5 physical + 2 team + 8 role); each value is `max(0, min(100, round(rng.gauss(mean, std_dev))))`. Draws are made in `_STAT_FIELDS` order to keep RNG consumption deterministic given a seeded `random.Random`. The module does **not** range-check `mean` / `std_dev` — that is the form's job.
- `draw_preferred_roles(rng) -> list[str]` — returns 1–3 unique role names drawn from the module-local `_ROLE_NAMES` tuple `("commander", "heavy", "scout", "medic", "ammo")` with a `70 / 20 / 10` count distribution. Implementation pin (so tests can assert RNG-consumption parity): `n = rng.choices([1, 2, 3], weights=[70, 20, 10], k=1)[0]` then `rng.sample(_ROLE_NAMES, n)`.
- `assign_slots(preferred_roles_per_player) -> dict[str, int | None]` — deterministic greedy bipartite match of the first 6 players to the 6-slot `_SLOT_KEYS` tuple `("commander", "heavy", "scout_1", "scout_2", "medic", "ammo")`. Canonical-slot-first iteration; the slot's role is the key with any trailing `"_1"` / `"_2"` stripped (so both Scout slots want `"scout"`); lowest-unassigned-player-index tie-break (pinned by `test_assign_slots_deterministic_tiebreak`); unmatched slots receive `None` and the view back-fills them with leftover players in ascending player-index order. **Pure: no `random` calls inside `assign_slots`** — the function is deterministic given its input; the RNG enters at the level above when the view shuffles player order before assembling the input.

**`_STAT_FIELDS` canonical order (frozen, 19 entries).** `player_awareness, game_awareness, resource_awareness, decision_making, positioning, stamina, speed, flexibility, adaptability, communication, teamwork, Offensive_synergy, defensive_synergy, midfield_synergy, resupply_synergy, resupply_efficiency, accuracy, survival, special_usage`. **`Offensive_synergy` is intentionally capital-O** — the existing field name on `Player` in `teams/models.py` uses that casing and the view's `Player(**stats)` splat requires the keys to match the field names byte-for-byte. The pure module hand-rolls `_ROLE_NAMES` and `_SLOT_KEYS` locally rather than importing `matches.sim_helpers.role_constants` — keeping `teams/player_generator.py` Django-free is the contract (the role-name strings happen to align with `PlayerRoundState.role` and `Player.preferred_roles` lowercase values).

**Form.** `GenerateLeagueForm` (`teams/forms.py`) has four fields: `num_teams` (`CharField` + `Select` widget, 22 choices `"0"` + `"2".."20"` + `"random_2_10"` — **`"1"` is deliberately omitted**), `players_per_team` (`CharField` + `Select` widget, 98 choices spanning `"6".."9"` + `"12".."100"` + `"random_team"` + `"random_pool"` — the wide superset is rendered always; **no JS-driven dependent dropdown for v1** and `clean()` enforces the cross-field rule), `mean` (`IntegerField`, `min_value=0`, `max_value=100`, `initial=50`), `std_dev` (`IntegerField`, `min_value=1`, `max_value=40`, `initial=15`). Cross-field validation in `clean()` raises `forms.ValidationError` with locked wording — `"Players per team must be 12–100 when generating a free-agent pool"` when `num_teams == "0"` paired with anything outside `{"random_pool"}` ∪ `{12..100}`, and `"Players per team must be 6–9 when generating teams"` when `num_teams != "0"` paired with anything outside `{"6", "7", "8", "9", "random_team"}`. Tests substring-match the locked copy.

**View.** `generate_players(request)` is decorated `@transaction.atomic` so the entire POST handler runs in one transaction — a mid-loop raise rolls back every Team and Player write (pinned by `test_pure_module_raises_mid_generation_rolls_back`). On `GET` → render `templates/teams/generate_players.html` with `{"form": GenerateLeagueForm()}` (status 200). On invalid `POST` → re-render the same template with the bound form and validation errors (status 200, no DB writes). On valid `POST` → resolve `random_*` markers via `random.randint(2, 10)` (`num_teams == "random_2_10"`) / `random.randint(6, 8)` (`players_per_team == "random_team"`) / `random.randint(12, 100)` (`players_per_team == "random_pool"`), build a fresh `rng = random.Random()` (production uses fresh entropy; **no seed input field on the form, deliberately out of scope**), pre-shuffle copies of `TEAM_NAMES` and `PLAYER_NAMES`, and pop from them as Teams/Players are created with the **shuffle-and-suffix collision policy** — on `Team.objects.filter(name=candidate).exists()` (or, for the free-agents branch, per-team `Player.objects.filter(team=free_agents_team, name=candidate).exists()`) the candidate gets `" #{k}"` with `k` incrementing from 2 until a free name is found; on pool exhaustion fall back to `f"{POOL[-1]} #{n}"`. The POST response **re-renders `generate_players_done.html` directly** (no redirect, no session round-trip) with context `{"created_teams": list[Team], "free_agent_count": int}`, status 200.

**Regular-teams branch (`num_teams ≥ 1`).** For each Team: pop a name + collision-check; `team = Team.objects.create(name=...)`. Pop `players_per_team` player profiles via the existing `_random_player_profile()` helper (**NOT** refactored to accept a `name` parameter — the view discards the helper's `name` field and overrides it from the popped+deduped Player-pool name). Build stats with `draw_stats(rng, mean, std_dev)` and `preferred_roles` with `draw_preferred_roles(rng)`. Create each Player via `Player.objects.create(team=team, name=..., **profile_minus_name, **stats, preferred_roles=...)`. Run `assign_slots(...)` on the first 6 players' preferred_roles lists; for each `(slot_key, player_index)` entry set the matching FK on the Team (`scout_1` → `team.slot_scout_1`, etc.); for `None`-valued slot_keys, back-fill with leftover players (those whose index was never assigned) in ascending player-index order. After back-fill, all 6 slots are filled. Players 7+ (`players_per_team > 6`) are bench: created on the Team but never assigned to a `slot_*` FK. `team.save()` once after all slot FKs are set.

**Free-agents branch (`num_teams == "0"`).** `free_agents = get_free_agents_team()`. Pop `players_per_team` player profiles (with per-team name collision-check against `free_agents`). For each, `Player.objects.create(team=free_agents, ...)` with stats and preferred_roles as above. **Do NOT touch any `slot_*` FK on the Free Agents Team** — it stays an unfilled roster on purpose (`is_valid_roster` returns False, by design — the Free Agents Team is a player container, not a competitive roster).

**Manager + helper.** `teams/models.py` gains a new `TeamManager(models.Manager)` class with a single public method `regular()` returning `self.exclude(name="Free Agents")`, assigned as `objects = TeamManager()` on the `Team` class. **No migration** — managers are not schema; Django picks the swap up at class load. **`Team.objects.all()` is unchanged** and continues to include the Free Agents Team — the distinction is pinned by `test_objects_all_still_includes_free_agents`. A new module-level helper `get_free_agents_team() -> Team` is defined adjacent to the `Team` class (mirrors the existing `_random_player_profile` module-level pattern, **NOT** a classmethod — keeps the call-site grep `get_free_agents_team()` short and obvious) returning `Team.objects.get_or_create(name="Free Agents")[0]`. Idempotent across runs — the second call returns the same `pk`. The Free Agents Team has no `slot_*` FKs filled (`is_valid_roster` returns False, by design). `teams/views.py::team_list` is migrated from `Team.objects.all()` to `Team.objects.regular()` so the Free Agents row never appears on `GET /teams/` — **out of scope** to migrate other `Team.objects.all()` call sites (admin / REST API / `simulate_match`) since any code that iterates rosters or simulates against the Free Agents Team already fails the `is_valid_roster` gate.

**Constants.** `teams/constants.py` gains a new top-level `TEAM_NAMES: tuple[str, ...]` of 30–50 themed laser-tag team names — same shape and casing as the existing `PLAYER_NAMES` (module-scope `tuple[str, ...]` of plain strings, all entries unique, no surrounding whitespace). `TEAM_NAMES` is consumed **only by the view** (`teams/views.py`); the pure module (`teams/player_generator.py`) does **NOT** import it per the pure-module import allowlist above.

**Templates.** NEW `templates/teams/generate_players.html` extends `base.html` and renders the four form fields with locked DOM ids `generate-players-form` (the `<form>`), `generate-players-num-teams` (`num_teams` `<select>`), `generate-players-per-team` (`players_per_team` `<select>`), `generate-players-mean` (`mean` `<input>`), `generate-players-std-dev` (`std_dev` `<input>`), `generate-players-submit` (the submit `<button>`). NEW `templates/teams/generate_players_done.html` extends `base.html` and renders the confirmation surfaces: a `<ul id="generate-confirm-teams-list">` (rendered only when `created_teams` is non-empty) listing each created Team as a `team_detail` link, and a `<div id="generate-confirm-free-agents-notice">` (rendered only when `free_agent_count > 0`) containing a `<span id="generate-confirm-free-agent-count">{{ free_agent_count }}</span>` and the locked copy substrings `"Created"` … `"free-agent players"` and `"once it ships (LG-00c)"` (the deferred-feature mention). Entry point: a `"Generate Players"` anchor (DOM id `generate-players-link`) in `templates/teams/team_list.html` sibling to the existing "New Team" link, reversing `{% url 'generate_players' %}`.

**Players tab deferral.** A `/players/` index page is **deliberately out of scope** for LG-00 — that surface lands as **LG-00c · Sortable Players tab** (`?sort=&dir=asc|desc` server-side sort with HX-02 forgiving-fallback validation, plus a "Players" nav link in `base.html`). LG-00c is intentionally listed immediately after LG-00b in PLAN.md so the generated free-agent pool is browsable shortly after LG-00 lands; until then, the free-agent count on the confirmation page is the only LG-00 surface that mentions the pool.

**Determinism / scope.** The pure module never seeds a global `random` and never touches simulator RNG; `@transaction.atomic` covers the entire POST handler so partial generation never persists. **No simulation behaviour change, no `_flush_to_db` touch, no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline obligation, no model field change, no migration, no ADR** (decisions are reversible — manager-class swap + module-level helper + pure module + new templates). **Out of scope (deliberate):** no per-stat or per-role bell-curve presets (a single `mean`/`std_dev` covers all 19 stats); no `is_system` Team field (Free Agents identified by magic name only); no `/players/` Players tab (LG-00c, deferred); no preview-before-commit UI (POST writes immediately); no seed input field on the form; no CSV import (LG-00b, separate task); no Season / Tournament linkage (LG-01+); no JS-driven dependent dropdown toggle for v1; no refactor of `_random_player_profile()` to accept a `name` parameter. Two CONTEXT.md domain terms added under `### Teams and players`: **Free Agents Team** and **LG-00 generation**. Tests live in **three NEW files** under `teams/tests/`: `test_player_generator.py` (pure-unit, no Django imports — `TestDrawStats`, `TestDrawPreferredRoles`, `TestAssignSlots`, `TestNoDjangoImportsLeaked`), `test_generate_view.py` (Django `TestCase` — `TestGenerateGet`, `TestGeneratePostHappyPathTeams`, `TestGeneratePostHappyPathBenchPlayers`, `TestGeneratePostHappyPathFreeAgents`, `TestGeneratePostRandomResolutions`, `TestGeneratePostCrossFieldValidation`, `TestGeneratePostNameCollisions`, `TestGeneratePostTransactionAtomic`, `TestFreeAgentsTeamAutoCreated`), and `test_team_list_filters_free_agents.py` (Django `TestCase` — `TestObjectsRegularManagerMethod`, `TestTeamListExcludesFreeAgents`).

Locked names — URL `path("generate/", views.generate_players, name="generate_players")` (full URL `/teams/generate/`); view `teams.views.generate_players`; form `teams.forms.GenerateLeagueForm`; pure module `teams/player_generator.py` with `draw_stats(rng, mean, std_dev) -> dict[str, int]` / `draw_preferred_roles(rng) -> list[str]` / `assign_slots(preferred_roles_per_player) -> dict[str, int | None]`; module-level tuples `_STAT_FIELDS` (19, order pinned above) / `_ROLE_NAMES` (5: commander, heavy, scout, medic, ammo) / `_SLOT_KEYS` (6: commander, heavy, scout_1, scout_2, medic, ammo); manager `teams.models.TeamManager` (assigned to `Team.objects`) with `Team.objects.regular()` excluding `name="Free Agents"`; helper `teams.models.get_free_agents_team() -> Team` (module-level, not a classmethod); Free Agents magic name `"Free Agents"`; constants tuple `teams.constants.TEAM_NAMES: tuple[str, ...]` (30–50 entries); templates `templates/teams/generate_players.html` (form) + `templates/teams/generate_players_done.html` (confirm); DOM ids `generate-players-form` / `generate-players-num-teams` / `generate-players-per-team` / `generate-players-mean` / `generate-players-std-dev` / `generate-players-submit` / `generate-players-link` (entry-point on team_list) / `generate-confirm-teams-list` / `generate-confirm-free-agents-notice` / `generate-confirm-free-agent-count`; test files `teams/tests/test_player_generator.py` + `teams/tests/test_generate_view.py` + `teams/tests/test_team_list_filters_free_agents.py`; CONTEXT.md terms added **Free Agents Team** + **LG-00 generation**; PLAN.md new task inserted **LG-00c · Sortable Players tab** (after LG-00b). Pinned by the seam contract at [`.claude/worktrees/lg-00-seam-contract.md`](../../.claude/worktrees/lg-00-seam-contract.md).

## LG-00b roster import

CSV-driven roster-import surface at `GET/POST /teams/import/` (URL name `import_roster`, view `teams/views.py::import_roster`) paired with a canonical template download at `GET /teams/import/template.csv` (URL name `import_roster_template`, view `teams/views.py::import_roster_template`). Both `path(...)` entries are appended to `teams/urls.py` **above** the existing `<int:team_id>/` capture-group routes so the literal `import/` segment is unambiguous. Tests reverse via `reverse("import_roster")` and `reverse("import_roster_template")` — no `app_name:` prefix (consistent with the existing `teams/urls.py` pattern). Entry point is a single `"Import Roster"` anchor (DOM id `roster-import-link`) added to `templates/teams/team_list.html` between the LG-00 `generate-players-link` and the existing `Create New Team` button.

**Pure parsing module.** `teams/roster_importer.py` is the algorithmic seam — **pure Python, no Django imports, no ORM, no settings access, no I/O** (frozen import allowlist: `csv` + `io` + `dataclasses` + `typing` from stdlib only; **explicitly forbidden**: `django.*`, `teams.models`, `teams.forms`, `teams.views`, `teams.constants`, `teams.player_generator`, `matches.*`, and any I/O / `os.path` module). The "no Django imports leaked" defensive check mirrors the RES-04 / RV-03 / HX-01 / HX-02 / LG-00 precedent. The module's public surface is one function plus four dataclasses/exception:

- `parse_roster_csv(text: str) -> ParsedRoster` — single string argument, no `**kwargs`, no optional flags. Caller owns file reading + decoding; the function defensively strips a single leading `"﻿"` BOM before invoking `csv.DictReader` (belt-and-suspenders with the form's `utf-8-sig` decode). Header-level errors raise `RosterImportError` IMMEDIATELY without attempting per-row parsing; per-row coercion errors accumulate across the whole file and bundle into a single raise after the loop plus an extra in-file collision pass; on success returns `ParsedRoster` (coercion-valid + in-file-collision-free).
- `ParsedRow(row_num, team, name, role, profile, stats, preferred_roles)` — `frozen=True` dataclass. `profile` is a 5-key dict (`age, started_playing_age, total_games, home_site, height`) whose keys MUST match `Player` field names byte-for-byte so the view's `Player.objects.create(**row.profile, **row.stats)` splat lands. `stats` always carries all 19 `STAT_COLUMNS` keys, defaulted to `STAT_DEFAULT` (`50`) where the cell was blank or the column absent. `preferred_roles` is 0–5 unique lowercase role names.
- `ParsedRoster(rows, by_team)` — `frozen=True` dataclass. `rows` is the flat CSV-order list (used for diagnostics + raw iteration); `by_team` is the view's primary consumption shape, a `dict[str, list[ParsedRow]]` whose insertion order matches first-appearance order of each team in the file (Python 3.7+ dict insertion-order guarantee — locked).
- `RowError(row_num: int, field: str | None, message: str)` — `frozen=True` dataclass, hashable so tests may put them in sets. `row_num` is **1-based DATA row index**: the first data row (line 2 of the file) is `row_num=1`; the header is line 1; file-level errors use `row_num=0` so the template sorts errors top-to-bottom with file-level first. `field` is `None` only for whole-row or whole-file errors.
- `RosterImportError(Exception)` with constructor `__init__(self, errors: list[RowError])` exposing `.errors`. The view re-renders this list verbatim and may **extend** it with DB-level slot collisions detected during the pre-flight check before re-raising.

**Module constants (frozen, hand-rolled locally — no import from `player_generator`).** `ROLE_NAMES = ("commander", "heavy", "scout", "medic", "ammo")`; `SLOT_LIMITS = {"commander": 1, "heavy": 1, "scout": 2, "medic": 1, "ammo": 1}`; `STAT_COLUMNS` is the 19-tuple matching `teams/player_generator.py::_STAT_FIELDS` verbatim — including the **intentional capital-O** `Offensive_synergy` so the view's `Player(**stats)` splat lands on the matching field — pinned by `test_stat_columns_equals_player_generator_stat_fields` (the ONE allowed `teams.player_generator` import inside the pure-unit test file); `REQUIRED_COLUMNS` is the 8-tuple `("team", "name", "role", "age", "started_playing_age", "total_games", "home_site", "height")`; `OPTIONAL_COLUMNS = ("preferred_roles", *STAT_COLUMNS)` (20 entries); `ALL_COLUMNS = (*REQUIRED_COLUMNS, *OPTIONAL_COLUMNS)` (28 entries — the declared order that drives the template-CSV header row); `PROFILE_BOUNDS = {"age": (5, 100), "started_playing_age": (3, 100), "total_games": (0, 100_000)}`; numeric constants `STAT_DEFAULT = 50`, `STAT_MIN = 0`, `STAT_MAX = 100`, `MAX_DATA_ROWS = 1000`, `NAME_MAX_LEN = 100`, `TEAM_NAME_MAX_LEN = 100`, `HOME_SITE_MAX_LEN = 100`, `HEIGHT_MAX_LEN = 20`.

**Coercion + error policy.** Per-cell rules: `team` / `name` stripped + non-empty + ≤100 chars; `role` lowercased + stripped + must be in `ROLE_NAMES` (so `"COMMANDER"` succeeds and lowercases); the three int profile fields use `int()` + `PROFILE_BOUNDS`; `home_site` / `height` stripped strings, empty allowed (becomes `""`); `preferred_roles` split on `","` + each entry stripped + lowercased + each non-empty entry in `ROLE_NAMES` + no duplicates within the cell (empty cell or column absent → `[]`); each of the 19 stat columns: cell omitted / blank / column absent → `STAT_DEFAULT`, else `int()` bounded `[STAT_MIN, STAT_MAX]`. **Header-level errors** (missing required column, unknown column, duplicate column, > `MAX_DATA_ROWS` data rows) raise `RosterImportError` IMMEDIATELY with `row_num=0` and skip per-row parsing — there is no point reading rows under a bad header. **Per-row coercion errors** accumulate across the whole file (no per-row early return) and are joined by an **in-file collision pass** that emits four hard-reject categories before the single bundled raise: duplicate `(team, name)` pair (`field="name"`), > 1 row of any non-Scout role on the same team (`field="role"`, message contains `"Too many"`, fires on the 2nd occurrence), > 2 Scouts on the same team (`field="role"`, fires on the 3rd occurrence), and slot-overflow detected by the view's DB pre-flight pass against pre-existing Teams (appended to the same `RosterImportError.errors` before any write — see view section below).

**Form.** `RosterImportForm(forms.Form)` is appended to `teams/forms.py` with a single `csv_file: forms.FileField` (widget id `roster-import-file`, `accept=".csv,text/csv"`). The form-class constant `RosterImportForm.MAX_UPLOAD_BYTES = 2 * 1024 * 1024` is the **only** byte cap (= 2 MiB exactly, comfortably > 1000 rows — row-cap enforcement is the pure module's job, not the form's). `clean_csv_file` decodes the upload with `bytes.decode("utf-8-sig")` (BOM-tolerated, belt-and-suspenders with the pure module's defensive `﻿` strip) and stores the **decoded `str`** on `cleaned_data["csv_file"]` so the view consumes text, never an `UploadedFile`. Locked error wording substrings: `"too large"` (file-too-large) and `"must be UTF-8"` (non-UTF-8) — tests substring-match both.

**View.** `import_roster(request)` is decorated `@transaction.atomic` so the entire POST handler runs in one transaction. The catch block uses **explicit `transaction.set_rollback(True)`** before re-rendering the form page — mandatory because the view returns a 200 response after catching `RosterImportError`, which without the explicit rollback would NOT trigger `@transaction.atomic`'s automatic rollback on raise; pinned by `test_db_slot_collision_rolls_back_all_writes`. On GET → render `templates/teams/roster_import.html` with `{"form": RosterImportForm(), "errors": [], "row_errors": []}` (status 200). On invalid form POST → re-render with the bound form (status 200, no DB writes). On valid POST, in order: (1) `parsed = parse_roster_csv(form.cleaned_data["csv_file"])`; (2) `_check_db_slot_collisions(parsed)` (its name is pinned for test monkey-patching stability — iterates `parsed.by_team.items()`, skips teams that do not exist, and for each existing team emits one `RowError` per DB-side slot collision before re-raising `RosterImportError`); (3) `created, appended, player_count = _apply_roster(parsed)` (also pinned — per Team in CSV-encounter order, `Team.objects.get_or_create(name=team_name)` auto-creates missing Teams while existing Teams are appended to; a team appears in only ONE of `created` / `appended` per call; each Player is created via `Player.objects.create(team=team, name=row.name, preferred_roles=row.preferred_roles, **row.profile, **row.stats)`; slot FK is `slot_<role>` for non-Scout or first-free of `slot_scout_1` / `slot_scout_2` for Scout, set in memory before `team.save()` once per team); (4) render `templates/teams/roster_import_done.html` with context `{"created_teams": list[Team], "appended_teams": list[Team], "player_count": int, "row_count": int}` (status 200, no redirect, no session round-trip). Any `RosterImportError` caught around steps 1–3 re-renders the form page with `{"form": form, "errors": [str(exc)], "row_errors": exc.errors}` (status 200). `import_roster_template(request)` is undecorated GET-only in practice — returns `HttpResponse(body, content_type="text/csv")` with `response["Content-Disposition"] = 'attachment; filename="roster_template.csv"'`; body is built via `csv.writer` (NOT manual string formatting) so the `"scout,medic"` cell on the example Scout row is auto-quoted correctly and the body terminates with `csv.writer`'s default `"\r\n"`.

**Templates.** NEW `templates/teams/roster_import.html` extends `base.html` with locked DOM ids `roster-import-form` (the `<form>`), `roster-import-file` (the `<input type="file">`), `roster-import-submit` (submit button), `roster-import-template-link` (the template-download anchor), `roster-import-errors-summary` (top-level errors `<div>`, rendered only when `errors` non-empty), `roster-import-errors` (per-row errors `<ul>`, rendered only when `row_errors` non-empty), and per-error `<li id="roster-import-error-{row_num}-{field|'row'}">` — the suffix is the literal field name when `err.field is not None`, otherwise the literal string `"row"` (e.g. `RowError(row_num=3, field="age", ...)` → `id="roster-import-error-3-age"`; `RowError(row_num=5, field=None, ...)` → `id="roster-import-error-5-row"`). NEW `templates/teams/roster_import_done.html` extends `base.html` with locked DOM ids `roster-import-confirm-summary` (the `Imported <strong>{{ player_count }}</strong> players across <strong>{{ row_count }}</strong> rows` `<div>`, locked copy substrings `"Imported"` … `"players across"` … `"rows"`), `roster-import-confirm-teams-list` (created-teams `<ul>` of `team_detail` links, rendered only when `created_teams` non-empty), and `roster-import-confirm-appended-list` (appended-teams `<ul>`, rendered only when `appended_teams` non-empty); a `"Back to Teams"` link reverses `team_list`, an `"Import another"` link reverses `import_roster`. Template-CSV body is frozen byte-for-byte (line 1: the 28-column header in `ALL_COLUMNS` order; lines 2–3: two example rows for a single team `"Red Phoenix"` — a Commander and a Scout with the comma-split `preferred_roles="scout,medic"` cell — so the file demonstrates the team-grouping behaviour, the comma-split `preferred_roles` quoting requirement, and a comma inside a quoted CSV cell); the test reconstructs the expected body via `csv.writer` + `io.StringIO` rather than embedding a bytestring literal.

**Role-vs-preferred-roles column distinction.** The `role` column drives **slot assignment ONLY** (the `team.slot_<role>` FK); `preferred_roles` is a SEPARATE optional column (comma-separated within the cell) that populates the `Player.preferred_roles` JSON list — **the two columns do NOT cross-influence each other**. A Commander row whose `preferred_roles` cell omits `"commander"` still fills `slot_commander`; a Scout row whose `preferred_roles` lists `"commander,heavy"` still fills `slot_scout_1` (or `slot_scout_2`). This decouples slot membership (the competitive roster) from the per-Player `stat_for_simulation` boost (which keys off `preferred_roles`).

**Format + capacity guarantees.** Strict exact-match headers, case-sensitive (including the capital-O `Offensive_synergy`); UTF-8 with BOM tolerated; comma-delimited only via stdlib `csv.DictReader` (no dialect detection); 1000-row data cap (pure module, file-level error on row 1001); 2 MiB byte cap (form, `MAX_UPLOAD_BYTES`). CONTEXT.md adds the **Roster import** glossary term under `### Teams and players` (added inline during the grilling session — the LG-00b Docs agent does NOT re-add it).

**Determinism / scope.** Fully deterministic given the input file — no `random.*` calls in either the pure module or the view. `@transaction.atomic` covers the entire POST handler and `transaction.set_rollback(True)` inside the catch block guarantees all-or-nothing semantics even on a 200 error response (pinned by `test_db_slot_collision_rolls_back_all_writes` AND `test_parser_raise_writes_nothing`). **No simulation behaviour change, no `_flush_to_db` touch, no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline, no model field change, no migration, no ADR, no new dependency** (the pure module is stdlib-only). **Out of scope (deliberate):** no preview-before-commit UI (POST writes immediately on success); no per-team `/teams/<id>/import/` entry point (single global entry only); no editing the existing per-player Add Player flow (`player_add` view unchanged); no editing LG-00's `generate_players` view or `player_generator.py` (the only cross-link is the `STAT_COLUMNS == _STAT_FIELDS` equality pin); no Django admin / REST API changes (`teams/api_views.py`, `teams/serializers.py` untouched); no async / Celery / progress bar (the 1000-row cap makes the import sub-second, foreground under `@transaction.atomic`); no JS validation / live preview; no per-row commit (all-or-nothing); no CSV dialect detection; no multi-file upload; no clobber / overwrite mode for existing players (the import only CREATES new Players — updating an existing Player is not in scope, and `Player.unique_together = ["team", "name"]` enforces this at the DB layer as a hard backstop). Tests live in **two NEW files** under `teams/tests/`: `test_roster_importer.py` (pure-unit, no Django imports — `TestHeaderValidation`, `TestCoercion`, `TestPreferredRoles`, `TestInFileCollisions`, `TestMultiErrorAccumulation`, `TestRowErrorShape`, `TestParsedRosterShape`, `TestNoDjangoImportsLeaked`) and `test_roster_import_view.py` (Django `TestCase` — `TestImportRosterGet`, `TestImportRosterPostHappyPath`, `TestImportRosterPostDbSlotCollision`, `TestImportRosterPostFormErrors`, `TestImportRosterPostParseErrors`, `TestImportRosterPostAtomic`, `TestImportRosterTemplate`, `TestEntryPointLink`).

Locked names — URLs `path("import/", views.import_roster, name="import_roster")` (full URL `/teams/import/`) + `path("import/template.csv", views.import_roster_template, name="import_roster_template")` (full URL `/teams/import/template.csv`); views `teams.views.import_roster` + `teams.views.import_roster_template` + private helpers `teams.views._check_db_slot_collisions(parsed)` + `teams.views._apply_roster(parsed) -> (created, appended, player_count)`; form `teams.forms.RosterImportForm` with field `csv_file` and constant `RosterImportForm.MAX_UPLOAD_BYTES = 2 * 1024 * 1024`; pure module `teams/roster_importer.py` with `parse_roster_csv(text: str) -> ParsedRoster`; dataclasses `RowError(row_num: int, field: str | None, message: str)` (frozen) + `ParsedRow(row_num, team, name, role, profile, stats, preferred_roles)` (frozen) + `ParsedRoster(rows, by_team)` (frozen); exception `RosterImportError(Exception)` with `errors: list[RowError]`; module tuples `REQUIRED_COLUMNS` (8) + `OPTIONAL_COLUMNS` (20) + `STAT_COLUMNS` (19, equals `_STAT_FIELDS`) + `ALL_COLUMNS` (28) + `ROLE_NAMES` (5); module dicts `SLOT_LIMITS = {"commander":1, "heavy":1, "scout":2, "medic":1, "ammo":1}` + `PROFILE_BOUNDS = {"age":(5,100), "started_playing_age":(3,100), "total_games":(0,100_000)}`; module constants `STAT_DEFAULT=50, STAT_MIN=0, STAT_MAX=100, MAX_DATA_ROWS=1000, NAME_MAX_LEN=100, TEAM_NAME_MAX_LEN=100, HOME_SITE_MAX_LEN=100, HEIGHT_MAX_LEN=20`; templates `templates/teams/roster_import.html` (form) + `templates/teams/roster_import_done.html` (confirm); DOM ids `roster-import-form` / `roster-import-file` / `roster-import-submit` / `roster-import-template-link` / `roster-import-errors` / `roster-import-errors-summary` / `roster-import-error-{row_num}-{field|"row"}` / `roster-import-confirm-summary` / `roster-import-confirm-teams-list` / `roster-import-confirm-appended-list` / `roster-import-link` (entry-point on team_list); test files `teams/tests/test_roster_importer.py` + `teams/tests/test_roster_import_view.py`; CONTEXT.md term **Roster import** (already added in grilling — Docs agent does NOT re-add). Pinned by the seam contract at [`.claude/worktrees/lg-00b-seam-contract.md`](../../.claude/worktrees/lg-00b-seam-contract.md).

## LG-00c sortable players tab

Read-only, server-side-sort, paginated index of every Player (including the Free Agents pool — no special-case exclusion) at `GET /players/` (URL name `player_list`, view `teams/views.py::player_list`, template `templates/teams/player_list.html`). Three query params drive presentation: `?sort=<key>` (default `"team"`, 23 accepted keys), `?dir=<asc|desc>` (default `"asc"`, case-sensitive — `"ASC"` falls back to `"asc"`), and `?page=<int>` (default 1, `EmptyPage` / `PageNotAnInteger` → page 1). Invalid params never raise — every bad input is coerced to a default by the two forgiving-fallback helpers (HX-02 pattern), and `get_object_or_404` is not used (the view never 404s).

**URL include.** One new `path("", views.player_list, name="player_list")` is appended to `teams/player_urls.py` **after** the two existing entries — final order is `benchmarks/` → `<int:player_id>/stats/` → `""`. The trailing position is mandatory: the `<int:player_id>` capture group cannot match an empty path segment, but the explicit ordering pin prevents a future regex change from silently shadowing the new route. Reverse via the bare `reverse("player_list")` — no `app_name:` prefix, consistent with the existing HX-01 / HX-02 routes in the same file. The outer `path("players/", include("teams.player_urls"))` mount in `laserforce_simulator/urls.py` is reused unchanged.

**No new pure-aggregation module.** LG-00c **deliberately ships no `teams/*.py` aggregation seam** — the helpers and constants live inline at module scope in `teams/views.py`, contrasting with HX-01 (`teams/career_stats.py`), HX-02 (`teams/role_benchmarks.py`), LG-00 (`teams/player_generator.py`), and LG-00b (`teams/roster_importer.py`) which all ship a pure module. The test file imports `_coerce_sort`, `_coerce_dir`, `_SORT_KEYS`, `_SORT_KEYS_DISPLAY`, `_VALID_DIRS`, and `_PAGE_SIZE` directly from `teams.views`. The helper unit tests (`TestCoerceSortAndDir`) run under Django's `TestCase` runner because of the `teams.views` import — they are NOT pure-unit in the HX-01 / HX-02 sense (those have actual Django-free pure modules with `test_no_django_imports_leaked` defensive checks; LG-00c does not).

**Sort whitelist — 23 accepted keys total.** Four new module-level constants in `teams/views.py`, placed beside the existing HX-02 `_coerce_threshold` / `_coerce_display`. `_SORT_KEYS: dict[str, str]` is a **22-entry** dict literal (URL key → ORM target) — the 19 stat fields plus `name` / `team` / `overall_rating`; insertion order is declared order for clarity, tests treat membership not ordering. `_SORT_KEYS_DISPLAY: tuple[tuple[str, str], ...]` is the **23-entry** display tuple (the 22 ORM-backed keys PLUS the `preferred_roles` sentinel — that's how the template iterates the column order including the JSON-field column) in the locked display order `(name, team, preferred_roles, overall_rating, player_awareness, game_awareness, resource_awareness, decision_making, positioning, stamina, speed, flexibility, adaptability, communication, teamwork, offensive_synergy, defensive_synergy, midfield_synergy, resupply_synergy, resupply_efficiency, accuracy, survival, special_usage)` paired with human labels `(Name, Team, Preferred Roles, Overall, Player Aware, Game Aware, Resource Aware, Decision, Positioning, Stamina, Speed, Flexibility, Adaptability, Communication, Teamwork, Offensive Syn, Defensive Syn, Midfield Syn, Resupply Syn, Resupply Eff, Accuracy, Survival, Special Usage)`. `_VALID_DIRS = ("asc", "desc")` and `_PAGE_SIZE = 50` round out the constants.

**Capital-O `Offensive_synergy` casing quirk.** URL key `offensive_synergy` (lowercase alias) maps to ORM target `Offensive_synergy` (capital-O, matching the existing `Player.Offensive_synergy` field byte-for-byte and the `_STAT_FIELDS` / `STAT_COLUMNS` precedent set by LG-00 / LG-00b). Pinned by `test_sort_by_offensive_synergy_url_alias_maps_to_capital_O_field` — the test creates 3 Players with distinct `Offensive_synergy` values, GETs `?sort=offensive_synergy&dir=desc` with the lowercase URL key, and asserts the top row is the highest-value player.

**`preferred_roles` Python-side sentinel — the 23rd accepted key.** `"preferred_roles"` is NOT in `_SORT_KEYS` (the field is a JSON list with no ORM target). The view handles it in a separate branch: materialise via `rows = list(qs)`, then `rows.sort(key=lambda p: (",".join(p.preferred_roles or []), p.name), reverse=(direction == "desc"))`, then `Paginator(rows, _PAGE_SIZE)`. Empty `preferred_roles` joins to `""` which sorts to the **TOP** of asc (`""` is lexicographically less than any non-empty string); the secondary `player.name` component is the deterministic tiebreak. Pinned by `test_sort_by_preferred_roles_python_branch` — the test creates 3 Players with `preferred_roles` = `[]`, `["scout"]`, `["commander", "heavy"]` and GETs `?sort=preferred_roles&dir=asc`, asserting the empty-list player sorts first, then `"commander,heavy"`, then `"scout"`.

**Forgiving-fallback helpers (inline, mirror HX-02).** Two new module-level functions at the same scope as `_coerce_threshold` / `_coerce_display`. `_coerce_sort(raw: str | None, default: str = "team") -> str` returns `raw` if `raw in _SORT_KEYS or raw == "preferred_roles"`, else `default` — the accepted-key set is the union of `_SORT_KEYS` keys and the `preferred_roles` sentinel (inlined, not exposed as a module-level constant). `_coerce_dir(raw: str | None, default: str = "asc") -> str` returns `raw` if `raw in _VALID_DIRS`, else `default` — case-sensitive (so `"ASC"` falls back to `"asc"`, mirroring `_coerce_display`'s casing discipline). Both helpers return `str` (never `None`); tests pin direct-call behaviour (`TestCoerceSortAndDir`).

**View.** `player_list(request)` is undecorated, runs `_coerce_sort(request.GET.get("sort"))` and `_coerce_dir(request.GET.get("dir"))`, builds the queryset via `Player.objects.select_related("team").annotate(overall_rating_db=...)` where the annotation is `(sum of 19 F-fields) / 19.0` (using `F("Offensive_synergy")` with the capital-O to match the field; the `/ 19.0` ensures float semantics under the SQL backend). The ORM branch then runs `qs.order_by(prefix + _SORT_KEYS[sort], "name")` where `prefix = "" if direction == "asc" else "-"` — the secondary `"name"` tiebreak is appended unconditionally (even when sorting by name itself — harmless, keeps the branch uniform). The Python branch (`sort == "preferred_roles"`) materialises and sorts in memory as described above. `Paginator(qs_or_rows, _PAGE_SIZE)` handles pagination; `EmptyPage` / `PageNotAnInteger` fall back to page 1. The view assembles two URL-encoded helper strings via `request.GET.copy()` + `.pop()`: `querystring_without_page` (used by Previous / Next page links so sort + dir survive page navigation) and `querystring_without_sort_dir_page` (used by column-header `<a>` hrefs so clicking a header drops `page=` and resets to page 1). Context ships **exactly seven** frozen keys: `page_obj, paginator, sort, dir, sort_keys, querystring_without_page, querystring_without_sort_dir_page`. The ORM branch issues exactly one queryset materialisation (the `Paginator` `.count()` + `.page()` round-trips are part of the paginator contract); the Python branch materialises once via `list(qs)`. Imports added: `from django.db.models import F` and `from django.core.paginator import Paginator, EmptyPage, PageNotAnInteger` (any subset already present is reused).

**Template surface.** NEW `templates/teams/player_list.html` extends `base.html` with `{% block title %}Players{% endblock %}` and renders a `<div class="table-responsive">` wrapper (Bootstrap 5.3, already on `base.html`) around a single `<table id="player-list-table">`. The `<thead>` iterates `sort_keys` (the 23-entry `_SORT_KEYS_DISPLAY`) emitting one `<th id="player-list-th-{url_key}">` per column with an `<a href>` that flips direction on the active column (`asc` ↔ `desc`) and starts new columns at `asc`; the href is built from `querystring_without_sort_dir_page` plus `sort=<key>&dir=<next-dir>` so clicking any header drops `page=`. **Active-column arrow glyphs** are the locked Unicode `↑` (U+2191, asc) and `↓` (U+2193, desc), appended to the human label with a single space (`{{ label }} ↑` / `{{ label }} ↓`) — pinned by `test_active_column_renders_arrow_glyph` via direct substring presence. The `<tbody>` renders one row per Player on the page: the `name` cell is `<a href="{% url 'player_career_stats' player.id %}">{{ player.name }}</a>` (HX-01 career-page link, pinned by `test_name_cell_links_to_career_stats`); the `team` cell is `<a href="{% url 'team_detail' player.team.id %}">{{ player.team.name }}</a>` (pinned by `test_team_cell_links_to_team_detail` — Free Agents Team players link to their own team detail page, no special-case, pinned by `test_free_agents_players_appear_in_listing`); the `preferred_roles` cell is `{{ player.preferred_roles|join:", " }}` (empty list renders an empty cell); the `overall_rating` cell uses the `@property` (NOT the annotation) via `{{ player.overall_rating|floatformat:1 }}` for consistency with the rest of the codebase; and the 19 stat cells render plain integers including the capital-O `{{ player.Offensive_synergy }}` attribute. The pagination block is a `<nav id="player-list-pagination">` rendered only when `page_obj.has_other_pages` is true, with Previous / Page X of Y / Next links whose hrefs use `querystring_without_page` so sort + dir are preserved across page navigation (pinned by `test_pagination_carries_sort_and_dir_in_links`); the column-header hrefs use `querystring_without_sort_dir_page` so clicking a header drops `page=` (pinned by `test_sort_change_resets_to_page_1`). The pagination test creates 51 Players so page 1 has 50 rows and page 2 has 1 row.

**`base.html` nav link.** Exactly **one new line** is added to `templates/base.html`: `<a class="nav-link" id="player-list-nav-link" href="{% url 'player_list' %}">Players</a>` placed in the existing `<div class="navbar-nav ms-auto">` block immediately AFTER the existing `Teams` `<a>` and BEFORE the existing `Matches` `<a>`. The new anchor uses `class="nav-link"` (same as siblings) — no CSS class change, no reordering of other nav links. Pinned by `test_nav_link_present_in_base_html` — the test GETs any view that extends `base.html` (the contract picks `team_list`), then asserts both substrings `"Players"` and `id="player-list-nav-link"` appear in the response body.

**Tests.** Live in **one NEW file** `teams/tests/test_player_list_view.py` with two classes and **27 tests total**. `TestCoerceSortAndDir` (10 helper tests — `test_coerce_sort_accepts_every_orm_key`, `test_coerce_sort_accepts_preferred_roles_sentinel`, `test_coerce_sort_falls_back_on_unknown_value`, `test_coerce_sort_falls_back_on_none`, `test_coerce_sort_falls_back_on_empty_string`, `test_coerce_dir_accepts_asc`, `test_coerce_dir_accepts_desc`, `test_coerce_dir_falls_back_on_unknown`, `test_coerce_dir_falls_back_on_none`, `test_coerce_dir_falls_back_on_uppercase`) — these import `_coerce_sort` / `_coerce_dir` / `_SORT_KEYS` / `_VALID_DIRS` directly from `teams.views` and run under Django's `TestCase` runner (they don't strictly need a DB, but the file inherits the Django test runner because of the `teams.views` import). `TestPlayerListView` (17 Django `TestCase` view tests via `reverse("player_list")` + the test client, real ORM, no mocks — `test_get_returns_200_with_default_sort`, `test_default_sort_is_team_asc_with_name_secondary` (pins the `Alpha/Aaron, Alpha/Zed, Bravo/Aaron, Bravo/Zed` row order), `test_sort_by_name_asc`, `test_sort_by_overall_rating_desc`, `test_sort_by_offensive_synergy_url_alias_maps_to_capital_O_field`, `test_sort_by_preferred_roles_python_branch`, `test_sort_by_every_stat_key_returns_200` (parametrised loop over every `(url_key, _)` in `_SORT_KEYS_DISPLAY` for both directions), `test_invalid_sort_falls_back_to_team`, `test_invalid_dir_falls_back_to_asc`, `test_pagination_renders_50_per_page` (51 Players, 50 on page 1, 1 on page 2), `test_pagination_carries_sort_and_dir_in_links`, `test_sort_change_resets_to_page_1`, `test_free_agents_players_appear_in_listing` (via `get_free_agents_team()`), `test_name_cell_links_to_career_stats`, `test_team_cell_links_to_team_detail`, `test_active_column_renders_arrow_glyph` (asserts `Name ↑` for `?sort=name&dir=asc` and `Name ↓` for `?sort=name&dir=desc`), `test_nav_link_present_in_base_html`).

**Determinism / scope.** Read-only view — no RNG, no simulation, no `_flush_to_db` touch, **no SIM-07 / SIM-08 contract interaction, no Score Calibration re-baseline obligation**. **No model field change, no migration, no ADR** (decisions are reversible — read-only view + inline helpers + new template + one-line `base.html` nav link addition). **No new pure-aggregation module** (the helpers live inline; LG-00c is the first read-only Players-area surface to deliberately skip the pure-module pattern — contrast with HX-01 / HX-02 / LG-00 / LG-00b which all ship one). **No new dependency** — Django stdlib `Paginator` + `F` are already imported / available. **No JS** — sort headers are plain anchor tags with query-string flips. The view is fully deterministic given the (Player, Team) DB state and the query string — same DB + same URL → identical rendered HTML. CONTEXT.md change is minimal: the existing **Free Agents Team** glossary entry's trailing `(deferred)` qualifier inside the `(LG-00c, deferred)` parenthetical is dropped — that is the ONLY CONTEXT.md change LG-00c is allowed to make; **no new domain term**. **Out of scope (deliberate):** no filter UI (no role filter, no team filter, no search box); no client-side / JS sort, no DataTables, no AJAX; no CSV export from this page; no per-player Edit / Delete buttons in rows (the existing `/teams/<team_id>/player/<player_id>/edit/` flow is unchanged); no bulk-actions checkbox column; ~~no alternative page sizes (`?per_page=` is not read);~~ **superseded — `?per_page=` ADDED in-PR**, see the per-page-selector paragraph below; no `is_simulated` toggle / filter; no API / JSON endpoint at `/players/` (the existing read-only DRF `/api/players/` already covers programmatic access; this is purely the HTML index); no CSV import / LG-00b coupling; no batch-sim / Celery touch.

Locked names — URL `path("", views.player_list, name="player_list")` (full URL `/players/`); view `teams.views.player_list(request)`; helpers `teams.views._coerce_sort(raw, default="team") -> str` + `teams.views._coerce_dir(raw, default="asc") -> str`; constants `teams.views._SORT_KEYS: dict[str, str]` (22 ORM-backed entries) + `teams.views._SORT_KEYS_DISPLAY: tuple[tuple[str, str], ...]` (23 column-display entries) + `teams.views._VALID_DIRS = ("asc", "desc")` + `teams.views._PAGE_SIZE = 50`; default sort key `"team"` + default direction `"asc"`; secondary tiebreak `"name"` (asc) — always appended to the ORM `.order_by()` and used in the Python-sort key tuple; sentinel sort key `"preferred_roles"` (Python branch); capital-O casing quirk URL key `"offensive_synergy"` → ORM target `"Offensive_synergy"`; annotation `overall_rating_db = (sum of 19 F-fields) / 19.0`; template `templates/teams/player_list.html`; nav-link line in `templates/base.html`; DOM ids `player-list-table` / `player-list-th-{url_key}` (e.g. `player-list-th-offensive_synergy`, `player-list-th-preferred_roles`) / `player-list-pagination` / `player-list-nav-link`; arrow glyphs `↑` (U+2191) + `↓` (U+2193); context keys (7) `page_obj, paginator, sort, dir, sort_keys, querystring_without_page, querystring_without_sort_dir_page`; test file `teams/tests/test_player_list_view.py` with classes `TestCoerceSortAndDir` (10 helper tests) + `TestPlayerListView` (17 view tests, 27 tests total); CONTEXT.md change drops the trailing `(deferred)` on the existing **Free Agents Team** entry; PLAN.md marks LG-00c done. Pinned by the seam contract at [`.claude/worktrees/lg-00c-seam-contract.md`](../../.claude/worktrees/lg-00c-seam-contract.md).

**Per-page selector (`?per_page=` extension landed in-PR).** `?per_page=10|25|50|100` (default `10`) — the original seam contract was scope-out for `per_page` but the feature was added in the same PR as a user-requested follow-up. New module-level constants in `teams/views.py`: `_VALID_PAGE_SIZES: tuple[int, ...] = (10, 25, 50, 100)` and `_DEFAULT_PAGE_SIZE: int = 10` (the previous `_PAGE_SIZE = 50` constant is REMOVED — the page size is dynamic now). New helper `_coerce_per_page(raw: str | None, default: int = _DEFAULT_PAGE_SIZE) -> int` mirrors HX-02's `_coerce_threshold` truthy-int coerce discipline: parses via `int()`; on `TypeError` / `ValueError` / missing returns default; on numeric-but-out-of-whitelist (`0`, negatives, `37`, `9999`, `11`, `49`, `101`) returns default. The view reads `per_page = _coerce_per_page(request.GET.get("per_page"))` and passes it to BOTH `Paginator(...)` calls; both querystring helpers carry the coerced `per_page` value via `qs["per_page"] = str(per_page)` so it survives across page navigation AND across column-header re-sorts. The context dict gains **two new keys** — `per_page` (the coerced int) and `page_size_options` (the `_VALID_PAGE_SIZES` tuple, iterated by the template `<select>`) — bringing the total to **9** (was 7 pre-extension). The dropdown surface is a small `<form method="get" id="player-list-per-page-form" class="mb-3">` placed above the table containing `<input type="hidden" name="sort" value="{{ sort }}">` + `<input type="hidden" name="dir" value="{{ dir }}">` (so changing the page size does NOT reset the user's column ordering) + a `<label for="player-list-per-page-select">Show:</label>` + `<select name="per_page" id="player-list-per-page-select" onchange="this.form.submit()">` (auto-submit on change, mirrors HX-02's submit-button form pattern but with a single-control auto-submit UX) with one `<option value="{size}" {% if size == per_page %}selected{% endif %}>{size}</option>` per `_VALID_PAGE_SIZES` entry + a `<noscript><button type="submit">Apply</button></noscript>` fallback for JS-disabled clients. New DOM ids: `player-list-per-page-form` + `player-list-per-page-select`. Tests cover: `_coerce_per_page` accepts every whitelisted size (`TestCoerceSortAndDir.test_coerce_per_page_accepts_every_whitelisted_size`); falls back on `None` / empty / non-int / out-of-whitelist (`test_coerce_per_page_falls_back_on_none` + `_falls_back_on_empty_string` + `_falls_back_on_non_int` + `_falls_back_on_out_of_whitelist`); view renders default 10 on missing param (`test_default_pagination_is_10_per_page` — was `test_pagination_renders_50_per_page` pre-extension); every valid `?per_page=` value renders the expected row count (`test_per_page_25_50_100_each_render`); invalid values silently coerce to default (`test_invalid_per_page_falls_back_to_default`); dropdown marks exactly one `<option selected>` and exposes locked DOM ids (`test_per_page_select_marks_active_option`); pagination links carry `per_page=` across pages (`test_pagination_carries_sort_and_dir_in_links` + `test_pagination_carries_per_page_in_links`); column-header re-sort hrefs carry `per_page=` (the `test_sort_change_resets_to_page_1` assertion now checks `per_page=10` is in the column-header qs but `&page=` is not — the substring check uses `qs.startswith("page=") or "&page=" in qs` to avoid matching the literal `page=` substring inside `per_page=`).

**Inline fixes landed in the same PR.** Three additional inline fixes were made during the web smoke / responsive smoke after the initial parallel-agents land — captured in `issues.md` as **LG00c-7** (pagination Previous/Next hrefs were carrying uncoerced invalid `?sort=&dir=` values verbatim; fixed by assigning the COERCED values back into the `QueryDict` before `.urlencode()` so the rendered URL always reflects the canonical/coerced query; regression test `test_pagination_links_drop_invalid_sort_and_dir`), **LG00c-8** (at viewports ≥ 1320px the 23-column table was clamped to Bootstrap's `.container.mt-4` max-width PLUS a redundant inner `<div class="container mt-4">` in the template, wasting up to ~600px of viewport on a 1920px screen; fixed by dropping the redundant inner `.container` and applying `style="margin-left: calc(-50vw + 50%); margin-right: calc(-50vw + 50%); padding: 0 1rem;"` to the `.table-responsive` wrapper — the calc no-ops at small viewports, breaks the wrapper out of the outer container to span the full viewport at wide ones; verified at 720 / 800 / 1280 / 1920 / 2560px with zero wasted space at every size), and **LG00c-9** (a multi-line `{# ... #}` block was rendering literally as visible text — Django `{# #}` is single-line only; collapsed to a one-line `{# #}` comment).

## Tests

`teams/tests/` package — split by concern:
- `test_models.py` — roster validation (FIX-01 coverage)
- `test_serializers.py` — PlayerSerializer, PlayerInlineSerializer, TeamSerializer, TeamListSerializer
- `test_apis.py` — HTTP-level tests for `/api/teams/` and `/api/players/`
- `test_forms.py` — `PlayerForm` stat field completeness (all 19 fields present, defaults to 50) and save behavior
- `test_models.py` — `_random_player_profile()` output validation (keys, value ranges, source lists); `stat_for_simulation` boost logic; roster validation
- `test_career_stats.py` — HX-01 pure-unit tests for `teams/career_stats.py` (4 public functions, empty inputs, sum/sum tag-ratio direction, Heavy `SPECIAL_COST` fallback, `was_eliminated_at=1801` capping to 1800, role ordering, `rolling_mean` partial-then-full window, `points_trend` `(date, game_round_id)` tiebreaker, "no Django imports leaked" defensive check) **plus** Django `TestCase` view tests for `/players/<player_id>/stats/` (200 + 6 context keys, 200 empty state, 404 missing player, "Career stats" link on player detail)
- `test_role_benchmarks.py` — HX-02 pure-unit tests for `teams/role_benchmarks.py` (6 public functions × empty / single-player / multi-player populations; threshold filtering; `mvp` derived-stat per-round-mean rule; `accuracy` sum/sum ratio shape pinned against an asymmetric two-player case; subject-inclusion policy — standalone row equals overlay cell for the same player; percentile-of-max equals `100.0`; "no Django imports leaked" defensive check mirroring the HX-01 / RES-04 / RV-03 precedent)
- `test_role_benchmarks_view.py` — HX-02 Django `TestCase` view tests (`GET /players/benchmarks/` 200 + 5 `benchmark-table-{role}` ids in body; `?threshold=` + `?display=` query-param parsing; malformed-param fallback to defaults; `benchmark-no-data-notice` substring on empty DB; `— (need N+ rounds)` substring on below-threshold subjects; HX-01 page `/players/<id>/stats/` surfaces the new `per_role_with_benchmarks` / `min_rounds` / `display` / `stat_keys` context keys and the `role-benchmarks-link` anchor)
- `test_role_benchmarks_cache.py` — HX-02 cache-behaviour tests (`PlayerRoundState.save()` + `.delete()` bump `role_benchmark_version` via the `teams/signals.py` handler; `BatchSimulator._flush_to_db` invokes `invalidate_role_benchmarks()` so a `bulk_create` round still busts the cache; single-scan-fills-all-keys hit-rate check — second request after invalidation hits cache for every `(role, stat)`; threshold-independence — toggling `?threshold=` does not bust the cache)